# GENERAL
CORS_ORIGIN="https://tenant1.lms.localhost"
TEST_KEY=

# DATABASE
DATABASE_URL="postgres://postgres:guidebook@localhost:5432/guidebook"

# REDIS
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="secret"
JWT_REFRESH_SECRET="refresh"
# Access token expiration - must match ACCESS_TOKEN_EXPIRATION_TIME in token.service.ts (24h)
# The JWT token expiration must match the cookie expiration to prevent the cookie outliving the token
JWT_EXPIRATION_TIME="24h"

# GOOGLE
# If you want to enable Google OAuth, set GOOGLE_OAUTH_ENABLED to true in both web and api .env files
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_ENABLED=

# MICROSOFT OAUTH
# If you want to enable Microsoft OAuth, set MICROSOFT_OAUTH_ENABLED to true in both web and api .env files
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_OAUTH_ENABLED=

# SLACK
# If you want to enable Slack OAuth, set SLACK_OAUTH_ENABLED to true in both web and api .env files
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_OAUTH_ENABLED=

# MAILS
EMAIL_ADAPTER="mailhog"
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_EMAIL_FROM="noreply@mentingo.com"

# AWS
AWS_BUCKET_NAME=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# S3
S3_ENDPOINT=http://localhost:9100
S3_REGION=eu-central-1
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET_NAME=my-local-bucket

# BUNNY
BUNNY_STREAM_API_KEY=
BUNNY_STREAM_SIGNING_KEY=
BUNNY_STREAM_LIBRARY_ID=
BUNNY_STREAM_CDN_URL=
BUNNY_STREAM_TOKEN_SIGNING_KEY=

# STRIPE
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

#SENTRY
SENTRY_ENVIRONMENT=development
SENTRY_DSN=

# AI-SDK
OPENAI_API_KEY=

# BULLMQ
WORKER_CONCURRENCY=10

# 32 Bytes Base64 - REQUIRED FOR APP TO WORK
MASTER_KEY=

# TEST
DATABASE_TEST_URL=postgresql://postgres:guidebook@localhost:5432/guidebook_test
REDIS_TEST_URL=redis://localhost:6379/1

# Analytics secret (generate: openssl rand --base64 32)
ANALYTICS_SECRET=

# Runtime DB connection (RLS enforced). Used by the app at runtime.
LMS_DATABASE_URL="postgresql://lms_app_user:replace_with_strong_password@localhost:5432/guidebook"

# Migrator/DDL DB connection (admin/owner). Used by migrations and seed scripts.
MIGRATOR_DATABASE_URL="postgres://postgres:guidebook@localhost:5432/guidebook"

# Comma-separated list of dev tenant origins to allow + seed.
DEV_TENANT_ORIGINS="https://tenant1.lms.localhost,https://tenant2.lms.localhost,https://tenant3.lms.localhost"

# Additional origins allowed only for /api/integration/* routes.
INTEGRATION_CORS_ORIGINS=

# Luma Url
LUMA_BASE_URL=


# Chess engine — Arasan MIT UCI binary path (optional; falls back to builtin)
# ARASAN_PATH="C:\Program Files\Arasan\26.0\arasanx-64.exe"
