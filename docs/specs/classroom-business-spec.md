# Classroom (Lớp học) Business Spec

> Đặc tả này mô tả hành vi nghiệp vụ độc lập, viết ra sau khi khảo sát một hệ thống tham khảo cấp phép AGPL-3.0 (lila/lichess.org, module `clas`, xem `docs/research/lila/06-clas-teardown.md`). Không chứa mã nguồn hay văn bản trích dẫn trực tiếp từ hệ thống đó. Cơ chế kỹ thuật cụ thể (schema PostgreSQL, cách gate quyền, cách sinh username/mật khẩu) là thiết kế riêng của mentingo, khớp với kiến trúc hiện có (NestJS/Drizzle/RLS đa tenant), không sao chép cách lila làm (Scala/Play/MongoDB). Đây là roadmap nhiều đợt (C0–C8); tài liệu được cập nhật ở cuối mỗi đợt — xem "Test Evidence" và "Follow-up Work" để biết chính xác phần nào đã xong tại thời điểm đọc.

## Business Overview

Đợt L5 (`docs/specs/chess-class-management-business-spec.md`, PR #25, **nay superseded bởi tài liệu này**) mới làm một lát mỏng của "lớp học": 5 endpoint tiện ích quản trị tài khoản học sinh cờ vua, dựng tạm trên bảng `groups` (cohort HR của LMS, không có chủ sở hữu/archive/bảng tin/ghi chú riêng). Khảo sát lại module `clas` của lila (`docs/research/lila/06-clas-teardown.md`) cho thấy khoảng cách lớn: 41 route so với 5, 4 entity lớp học so với 0.

Roadmap này (**C0–C8**) xây một module **Classroom** đầy đủ, độc lập với `groups`, phục vụ mọi loại lớp học trong LMS (không chỉ cờ vua — cờ vua là một trong các "khóa học" có thể gán cho lớp). Đạt tính năng ngang lila `clas` (quản lý lớp nhiều giáo viên, mời/tạo học sinh hàng loạt, mã đăng nhập nhanh cho tài khoản trẻ em, bảng tin, báo cáo tiến độ, thao tác hàng loạt, chuyển lớp, graduate/đóng tài khoản), cộng thêm phần mở rộng riêng của mentingo mà lila không có: gán khóa học/bài tập/chứng chỉ cho lớp — xem mục "Ngoài phạm vi lila" bên dưới để phân biệt rõ hai loại.

## Who Uses It

- **Giáo viên** (bất kỳ ai có quyền `classroom.create` — mặc định vai trò `trainer`, `admin`): tự đăng ký làm giáo viên, tạo/sửa/đóng lớp, quản lý học sinh trong lớp mình dạy, xem báo cáo tiến độ, gán nội dung học.
- **Học sinh**: thành viên của một hay nhiều lớp — xem dashboard lớp, bảng tin, danh sách bạn cùng lớp; nếu là tài khoản do giáo viên quản lý (managed) thì đăng nhập bằng mã lớp hoặc username/mật khẩu được cấp.
- **Quản trị viên** (`admin`): toàn quyền như giáo viên trên mọi lớp, cộng thêm xem lớp của một giáo viên bất kỳ và xem thông tin định danh thật của học sinh cho mục đích giám sát.

## Feature Functions

### 1. Quản lý lớp (C1)

- Tạo/sửa/xem danh sách lớp — mỗi lớp có tên, mô tả, danh sách giáo viên (1–10, quyền ngang nhau), cờ cho-phép-nhắn-tin, trần số học sinh (mặc định 100, đọc từ settings).
- Archive (đóng mềm) / reopen. Đóng không xóa dữ liệu, chỉ ẩn khỏi danh sách học sinh nhìn thấy.
- Ghi nhận "lần xem gần nhất" mỗi khi giáo viên mở trang lớp — làm cơ sở cho auto-archive (C4).
- Mọi truy cập không thuộc quan hệ giáo viên/học sinh của lớp trả 404 (không lộ sự tồn tại của lớp).

### 2. Học sinh trong lớp (C3)

- Ba cách thêm học sinh: mời tài khoản có sẵn (4 nhánh phản hồi tùy trạng thái người được mời), tạo 1 tài khoản managed mới, tạo hàng loạt từ danh sách tên thật dán vào.
- Tài khoản managed: email nội bộ không nhận thư thật, mật khẩu ngẫu nhiên hiện **một lần**, bắt buộc kid mode, rating cờ khởi tạo thấp hơn mặc định (áp dụng khi lớp gắn nội dung cờ vua).
- Hồ sơ học sinh trong lớp: tên thật + ghi chú riêng (chỉ giáo viên của đúng lớp đó thấy).
- Archive/khôi phục học sinh, reset mật khẩu (đăng xuất mọi phiên), graduate (chuyển sang email thật → tự quản), đóng tài khoản vĩnh viễn, chuyển sang lớp khác (giữ nguyên hồ sơ).
- Thao tác hàng loạt: archive/restore/move/remove/delete-invites theo cơ chế "sửa danh sách để giữ lại, phần còn lại bị tác động".

### 3. Mã đăng nhập nhanh (C1/C3)

- Sinh một bộ mã 5 ký tự (bảng ký tự loại trừ ký tự dễ nhầm) cho toàn bộ học sinh managed+hoạt động của lớp, hết hạn 15 phút, dùng một lần, vô hiệu hóa bộ cũ khi sinh bộ mới.
- Đăng nhập công khai bằng mã, rate limit theo IP, không kiểm tra mật khẩu.

### 4. Bảng tin & thông báo lớp (C4)

- Bảng tin (wall) markdown do giáo viên soạn, học sinh chỉ xem.
- Gửi thông báo toàn lớp (10–300 ký tự, tự nối link lớp), chỉ khả dụng khi ≤ trần học sinh.
- Tự động archive lớp không được giáo viên xem quá 30 ngày (cron theo tenant, dry-run trước khi bật thật).

### 5. Báo cáo tiến độ (C6)

- Theo hạng mục × số ngày: rating đầu/cuối, số ván/bài, thắng, thời lượng, win rate, trung bình lớp.
- Tiến độ nội dung nhập môn (nếu lớp gắn nội dung cờ vua): % hoàn thành Learn/Practice/Coordinate.
- Tiến độ khóa học/bài tập LMS thường (nếu lớp gắn khóa học — mục 6).

### 6. Nối lớp với LMS — **ngoài phạm vi lila, mở rộng riêng của mentingo** (C5)

- Gán/gỡ khóa học cho lớp (ghi danh toàn bộ học sinh, giữ provenance qua `enrolled_by_classroom_id`).
- Giao bài tập (`assignments`) cho lớp.
- Nếu lớp gắn nội dung cờ vua: giao Study/Puzzle, mời hàng loạt vào giải đấu (không auto-join).

### 7. Quyền riêng tư & bảo mật (C1/C3/C7)

- Tên thật hiển thị có điều kiện qua một serializer duy nhất: chính chủ không thấy, giáo viên chỉ thấy học sinh lớp mình dạy, học sinh chỉ thấy bạn cùng lớp.
- Tài khoản managed: bắt buộc kid mode, không tự tắt được, không tự đóng tài khoản được, không tự đổi mật khẩu được.
- Kid-mode DM/follow chỉ giữa bạn cùng lớp (lớp bật `canMsg`) hoặc với giáo viên của mình — mở rộng cơ chế `community` sẵn có (Đợt L9), không viết lại.
- GDPR: xóa user → ghost hoá vai trò trong lớp, xóa hồ sơ học sinh liên quan.

## Ngoài phạm vi lila (không suy diễn từ hành vi lila — quyết định riêng của mentingo)

Gán khóa học/bài tập/chứng chỉ cho lớp (mục 6) là phần mở rộng có chủ đích, **lila không có khái niệm này** (`docs/research/lila/06-clas-teardown.md` mục F.1). Không làm: điểm danh, sổ điểm, thời khóa biểu — lila cũng không có, và đã chốt không mở rộng sang đó.

## Không port từ lila (đã cân nhắc, quyết định bỏ hoặc thay thế)

Xem bảng đầy đủ ở kế hoạch triển khai nội bộ; tóm tắt: bloom filter (thay bằng partial index Postgres), chặn Tor/proxy/firewall IP (đã có rate-limit + reverse proxy), đồng bộ lớp ↔ Team Lichess (mentingo không có khái niệm Team, dùng `chess_tournaments` sẵn có), auto-join giải đấu (đổi thành mời hàng loạt, không tự động), denormalize trường lên bản ghi ván đấu (tối ưu MongoDB-specific, Postgres JOIN đủ nhanh), xuất OAuth token học sinh (ngoài phạm vi chương trình học), metrics/modlog riêng (gộp vào `activity_logs`/`statistics` sẵn có).

## End-User Value

Trường/CLB dạy học có thể quản lý một lớp thật (nhiều giáo viên cùng dạy, học sinh nhỏ tuổi không cần email, đăng nhập bằng mã chiếu lên bảng, ghi chú riêng tư về từng em, chuyển lớp khi đổi giáo viên, đóng lớp cuối khóa mà không mất dữ liệu) — thay vì phải mượn tạm khái niệm "nhóm" của HR/L&D vốn không có các ngữ nghĩa này. Gán khóa học/bài tập cho lớp giúp lớp học không chỉ dừng ở quản lý tài khoản mà thực sự là nơi giao và theo dõi việc học.

## How It Works

Giáo viên (đã có quyền, hoặc tự đăng ký nếu đủ điều kiện) tạo một lớp, dán danh sách tên học sinh để tạo tài khoản hàng loạt hoặc mời tài khoản có sẵn. Đầu buổi học, giáo viên sinh mã đăng nhập nhanh và chiếu lên màn hình; học sinh gõ mã vào trang đăng nhập, vào thẳng hệ thống trong 15 phút. Giáo viên viết bảng tin, gửi thông báo, gán khóa học/bài tập cho lớp, xem báo cáo tiến độ định kỳ. Khi học sinh đủ lớn hoặc chuyển đi, giáo viên chạy "graduate" (chuyển sang tài khoản tự quản) hoặc "chuyển lớp". Lớp không dùng tới sẽ tự động được lưu trữ sau 30 ngày, không mất dữ liệu, mở lại được bất kỳ lúc nào.

## Key Technical Context

_(Mục này được bổ sung dần theo từng đợt C1–C8 — xem lịch sử git của file này để biết trạng thái tại một thời điểm cụ thể trong roadmap.)_

- Bảng dự kiến toàn roadmap: `classrooms`, `classroom_teachers`, `classroom_students`, `classroom_invites`, `classroom_announcements`, `classroom_courses`, `classroom_assignments` — tất cả độc lập với `groups` (không phụ thuộc, không mở rộng bảng đó).
- Bốn điểm neo `groupId` ẩn cần xử lý khi tách sạch khỏi `groups`: `community-social.repository.ts` (kid-mode DM), `group_announcements`, `group_courses`, `student_courses.enrolled_by_group_id`.
- Module backend: `apps/api/src/classroom/`. Mọi method controller prefix `classroom` (tránh trùng tên với `swagger-typescript-api` — đã có tiền lệ vấp lỗi này ở Đợt L2/L7/L10 và ở chính module `chess-class`).
- 5 endpoint `chess-class` cũ (Đợt L5) giữ làm alias deprecated trong C2, xóa hẳn ở C8 sau khi UI mới đã thay thế.

### Đợt C1 (hoàn tất)

- 3 bảng mới (`classrooms`, `classroom_teachers`, `classroom_students`) — migration `0201_add_classrooms` (drizzle-kit) + `0202_enable_classrooms_rls` (custom, RLS theo tenant). `classroom_students.classroom_id` có partial index `WHERE archived_at IS NULL` để tối ưu truy vấn "học sinh đang hoạt động" — bảng này chưa có ghi nào ở C1 (chưa có endpoint tạo học sinh, đó là phạm vi C3), nhưng schema đã sẵn sàng để `assertCanRead`/kid-mode dùng ngay.
- Module `apps/api/src/classroom/` (`classroom.controller.ts` + `.service.ts` + `.repository.ts` + `.module.ts` + `schemas/classroom.schema.ts` + `classroom.types.ts` + `__tests__/`), đăng ký vào `app.module.ts`.
- 8 endpoint: `createClassroom`, `listTeachingClassrooms`, `listLearningClassrooms`, `getClassroomDetail`, `updateClassroom`, `setClassroomArchived`, `addClassroomTeacher`, `removeClassroomTeacher` — không method nào trùng tên với method có sẵn trong `apps/api/src` (xác minh bằng test tự động, xem dưới).
- Permission mới: `classroom.read` (student/trainer/admin), `classroom.create` (trainer/admin), `classroom.manage` (admin — bỏ qua kiểm tra quan hệ, quản lý mọi lớp), `classroom.manage_own` (trainer — chỉ lớp mình dạy).
- Bất biến bảo mật đã cài đặt: gate ở controller bằng `classroom.read` (thô nhất), service tự quyết theo resource qua `assertCanRead`/`assertCanManage`; **luôn 404** cho người lạ (không phải giáo viên/học sinh của lớp), **403** cho học sinh-thành-viên cố sửa lớp (họ biết lớp tồn tại, chỉ không đủ quyền); trần 10 giáo viên/lớp (`CLASSROOM_DEFAULTS.MAX_TEACHERS`, `packages/shared/src/constants/classroom.ts`); không cho xóa giáo viên cuối cùng của lớp; `viewedAt` chỉ cập nhật khi **giáo viên** (không phải học sinh) mở trang lớp — cơ sở cho auto-archive ở C4.
- **Sửa `CommunitySocialRepository.shareGroup()` → đổi tên `sharesClassmateRelationship()`**, mở rộng raw SQL thành `UNION ALL` giữa `group_users` (giữ nguyên hành vi cũ) và `classroom_students` (active-only, mới thêm) — theo đúng yêu cầu bắt buộc của kế hoạch. Cập nhật `community-social.service.ts` (`assertCanInteract`) và toàn bộ mock trong `community-social.service.spec.ts`.
- CI check tự động: `apps/api/src/__tests__/no-duplicate-controller-method-names.spec.ts` — quét mọi `*.controller.ts`, chặn **method mới** trùng tên xuyên suốt `apps/api/src` (không chặn ~20 trùng tên đã tồn tại từ trước, liệt kê rõ trong file làm baseline).
- Test key-parity locale: `apps/web/app/locales/__tests__/classroomLocaleKeyParity.spec.ts` — đảm bảo namespace `classroom.*` giống hệt nhau ở cả 7 file locale (không cố sửa phần drift có sẵn từ trước ở các namespace khác — đó là nợ kỹ thuật không liên quan tới đợt này).
- Frontend: `apps/web/app/modules/Classroom/{ClassroomList,ClassroomDetail}.page.tsx`, route `classrooms` + `classrooms/:classroomId` (trong `PublicDashboard.layout` — không phải `Admin.layout`, vì học sinh cũng cần truy cập), `routeAccessConfig.ts` cập nhật, 8 hook TanStack Query (`api/queries/use{Teaching,Learning}Classrooms.ts`, `useClassroomDetail.ts`; `api/mutations/use{Create,Update,SetArchived,AddTeacher,RemoveTeacher}Classroom*.ts`).
- Chuỗi UI mới thêm vào đủ 7 file locale (`classroom.*` + 2 khóa `pages.classrooms`/`pages.classroomDetail`), dịch thật (không machine-copy).

### Đợt C2 (hoàn tất)

- **Migration schema (0203, generated)**: thêm 2 cột cầu nối, cả hai nullable, đúng nguyên tắc additive-only — `chess_class_login_codes.classroom_id` (FK `classrooms`, chưa nơi nào đọc) và `classrooms.source_group_id` (FK `groups`, unique). `sourceGroupId` **chỉ dùng cho di trú/redirect ngược**, không service/repository nào của Classroom đọc cột này để quyết định thành viên lớp — ghi rõ bằng comment tại chỗ khai báo trong `storage/schema/index.ts` để tránh bị hiểu nhầm là một phụ thuộc thật vào `groups`.
- **Migration data (0204, custom, idempotent)**: với mỗi `groups` đang có ít nhất 1 học sinh managed (Đợt L5) → tạo 1 `classrooms` (tên lấy từ `groups.name` locale en/vi, fallback "Untitled classroom"), owner = giáo viên quản nhiều học sinh nhất trong group đó (`users.managed_by_user_id`, phá hòa bằng `owner_id`) → 1 `classroom_teachers` (owner) → N `classroom_students` (mọi học sinh managed trong group, `realName` từ `users.real_name`/`username`/fallback "Học sinh") → backfill `chess_class_login_codes.classroom_id` từ mapping. Idempotent qua `ON CONFLICT (source_group_id) DO NOTHING` ở bước tạo lớp + `NOT EXISTS` ở bước tạo học sinh — chạy lại nhiều lần cho kết quả giống hệt. **Không** `UPDATE`/`DELETE` bất kỳ dòng nào trong `groups`/`group_users`.
- **5 endpoint `chess-class` cũ**: thêm `@ApiOperation({ deprecated: true, summary: "..." })` trỏ về spec này — chỉ đổi metadata Swagger, **hành vi giữ nguyên 100%** (vẫn đọc/ghi qua `groups`/`group_users` như trước, chưa chuyển sang `classroom_students` — việc đó thuộc C3 khi tính năng tạo học sinh chuyển hẳn sang Classroom module).
- **Endpoint cầu nối mới** `GET /classroom/by-source-group/:groupId` (`getClassroomIdForSourceGroup`) — chỉ để tra `classroomId` tương ứng cho mục đích redirect UI, không thuộc bề mặt tính năng Classroom, sẽ xóa cùng lúc dọn trang admin cũ ở C8.
- **UI "redirect"**: triển khai như **banner liên kết**, không phải điều hướng ép buộc — quyết định có chủ đích khác với chữ dùng gốc trong kế hoạch ("redirect"), vì tính năng tạo học sinh/mã đăng nhập/báo cáo tiến độ **chưa được port sang Classroom module** (đó là phạm vi C3–C6); ép điều hướng ngay bây giờ sẽ làm giáo viên mất đường vào chức năng duy nhất còn hoạt động. `ChessClassManagement.page.tsx` hiện banner "đang được thay thế" + nút "Xem lớp học tương ứng" khi backfill đã có lớp khớp; `EditGroup.page.tsx` thêm nút thứ hai cạnh "Manage chess class" với cùng điều kiện. Điều hướng ép buộc thật sẽ làm ở C8 khi trang cũ bị xóa hẳn.
- 2 khóa locale mới (`chessClass.deprecatedBanner.text`/`.link`) chỉ thêm vào **en/vi** — theo đúng hiện trạng namespace `chessClass` (vốn đã chỉ tồn tại ở 2 locale này từ Đợt L5, `pl`/`de`/`lt`/`cs`/`es` chưa từng có namespace này; đây là nợ kỹ thuật có sẵn, không phải do đợt này tạo ra, và nằm ngoài phạm vi `classroomLocaleKeyParity.spec.ts` vốn chỉ kiểm namespace `classroom.*` mới).

### Đợt C3 (hoàn tất)

- **Bảng mới `classroom_invites`** — migration `0205_add_classroom_invites` (generated) + `0206_enable_classroom_invites_rls` (custom). Cột: `classroomId`, `userId`, `realName`, `status` (`pending`/`accepted`/`declined`, `CLASSROOM_INVITE_STATUSES` trong `packages/shared/src/constants/classroom.ts`), `createdBy`, `tenantId`, timestamps. Unique index bán phần `(classroomId, userId) WHERE status = 'pending'` — cho phép mời lại sau khi đã từ chối/thu hồi, nhưng chặn 2 lời mời đang chờ cùng lúc cho cùng một cặp lớp/học sinh.
- **3 cách thêm học sinh**: `inviteStudent` (mời tài khoản có sẵn qua username, 4 nhánh phản hồi: đã là học sinh của lớp / đã có lời mời đang chờ / mời thành công / không tìm thấy username), `createStudent` (tạo 1 tài khoản managed), `bulkCreateStudents` (dán danh sách tên, mỗi dòng 1 học sinh) — cả ba đều chặn ở `assertRoomForStudents` khi vượt trần 100 học sinh/lớp (`CLASSROOM_DEFAULTS.MAX_STUDENTS`).
- Tài khoản managed mới dùng lại nguyên vẹn hạ tầng đã có của Đợt L5: sinh username duy nhất (`generateUniqueUsernames`, retry khi trùng), băm mật khẩu, `insertUserOnboardingRows`, `createSettingsForUsers` (bắt buộc kid mode), gán vai trò `SYSTEM_ROLE_SLUGS.STUDENT` — **không viết lại** logic này, chỉ gọi lại qua `ClassroomRepository`.
- **Quản lý học sinh trong lớp**: `listStudents`/`getStudentDetail`/`updateStudent` (sửa `realName`+`notes`, chỉ giáo viên của đúng lớp thấy được `notes`), `setStudentArchived` (archive/khôi phục), `resetStudentPassword` (chỉ tài khoản managed — chặn 403/400 nếu học sinh tự quản, đăng xuất mọi phiên qua thu hồi token, ghi `activity_logs`), `releaseStudent` (graduate: đổi email nội bộ sang email thật của học sinh, sinh `create_token` để học sinh tự đặt mật khẩu lần đầu, ghi `activity_logs`), `closeStudent` (đóng vĩnh viễn — chỉ tài khoản managed, gọi `UserService.bulkArchiveUsers` có sẵn, không viết lại), `moveStudent` (chuyển sang lớp khác, **giữ nguyên** `notes`/`archivedAt`/`archivedBy` qua tham số `preserve` mới thêm ở `insertClassroomStudent`).
- **Thao tác hàng loạt** (`runBulkStudentAction`): archive/restore/remove/move-to-X trên một tập học sinh được chọn qua checkbox — **khác thiết kế gốc của lila** (lila dùng textarea dán sẵn danh sách tên, cơ chế "ai còn trong danh sách thì bị tác động"); mentingo chọn checkbox multi-select vì phù hợp hơn với UI bảng dữ liệu sẵn có của LMS (`DataTable` dùng ở các trang Admin khác) — quyết định UX có chủ đích, không phải sao chép.
- **Lời mời**: `listClassroomInvites`/`revokeInvite` (phía giáo viên), `listMyInvites`/`acceptInvite`/`declineInvite` (phía học sinh được mời — `getOwnInviteOrThrow` trả 404 nếu không phải lời mời của chính mình, không lộ tồn tại); accept một lời mời khi đã từng decline trước đó (đổi ý) được cho phép — tạo lại `classroom_students` mới, không phục hồi bản ghi cũ.
- 17 endpoint mới trong `classroom.controller.ts`, tất cả tiền tố `classroom` (ví dụ `createClassroomStudent`, `listClassroomStudents`, `archiveClassroomStudent`, `resetClassroomStudentPassword`, `moveClassroomStudent`, `runClassroomBulkAction`, `inviteClassroomStudent`, `acceptClassroomInvite`) — xác minh không trùng tên qua `no-duplicate-controller-method-names.spec.ts` (baseline không đổi, không thêm exception nào).
- `ClassroomModule` nay import `SettingsModule`/`ActivityLogsModule`/`UserModule` để lấy `CLASSROOM_DEFAULTS` runtime (đọc từ settings, không hardcode), ghi `activity_logs` cho hành động nhạy cảm (reset mật khẩu, release, đóng tài khoản), và gọi `UserService.bulkArchiveUsers` có sẵn — xác minh **không** vấp lại lỗi circular-require từng gặp ở Đợt L3 khi import `src/chess/` (đã boot thật bằng `nest start` để xác nhận).
- Frontend: 5 trang mới (`ClassroomStudentsList`, `ClassroomAddStudent` — 3 khối invite/create-one/bulk-create trên cùng 1 trang, `ClassroomStudentDetail` — dialog reset-password/release/move/close, `ClassroomEditStudent`, `ClassroomBulkActions` — checkbox multi-select), 4 query hook + 12 mutation hook, 5 route mới đăng ký trong `routes.ts`/`routeAccessConfig.ts` (cùng layout `PublicDashboard` như C1, cùng permission `classroom.read`/`classroom.manage_own`/`classroom.manage`).
- i18n: toàn bộ chuỗi mới (`classroom.students.*`, `.addStudent.*`, `.studentDetail.*`, `.editStudent.*`, `.bulkActions.*`, ~15 khóa `toast` mới, ~13 khóa `error` mới, 5 khóa `pages.*` mới) dịch thật vào đủ **7** file locale trong chính đợt này (không dồn cuối) — `classroomLocaleKeyParity.spec.ts` xác nhận khớp 100% giữa `en` và 6 locale còn lại.

### Đợt C4 (hoàn tất)

- **Bảng tin (`wall`)**: cột `classrooms.wall` đã tồn tại sẵn từ C1 (không cần migration) — chỉ thêm `wall` vào `updateClassroomBodySchema`/`UpdateClassroomInput`, tái dùng nguyên vẹn `updateClassroom`/`assertCanManage` đã có (giáo viên ghi, học sinh chỉ đọc qua `classroomSchema` đã có sẵn field này). Frontend: `ClassroomDetail.page.tsx` thêm ô Markdown editor (giáo viên) + `Markdown` (react-markdown, đã là dependency có sẵn, dùng ở `ChatMessage.tsx`) hiển thị read-only (học sinh).
- **Thông báo toàn lớp**: **không** tạo bảng `classroom_announcements` mới như phác thảo ban đầu trong roadmap — khảo sát trước khi code phát hiện `announcements` đã có sẵn cặp cột generic `sourceType`/`sourceId` (dùng cho `live_training`) đúng để mở rộng, tránh phải thêm join table + sửa `getAnnouncementById`. Thêm `CLASSROOM: "classroom"` vào `ANNOUNCEMENT_SOURCE_TYPES`; `ClassroomService.sendAnnouncement` gọi thẳng `AnnouncementsSchedulerService.createSystemAnnouncement({ sourceType: "classroom", sourceId: classroomId, sendEmail: false, ... })` — tái dùng 100% pipeline tạo/publish/fan-out đã có, không viết lại. `AnnouncementsRepository.createUserAnnouncementRecordsForAnnouncement` thêm 1 nhánh mới `getClassroomAnnouncementRecipientIds` (UNION `classroom_teachers` ∪ `classroom_students` đang hoạt động, loại trừ chính người gửi) — dùng `union()` từ `drizzle-orm/pg-core` (mẫu lấy từ `unionAll` sẵn có trong `global-search/search-index.repository.ts`). Validate độ dài 10–300 ký tự qua `sendClassroomAnnouncementBodySchema` (`CLASSROOM_ANNOUNCEMENT_MIN/MAX_LENGTH` mới trong `packages/shared/src/constants/classroom.ts`); trần "chỉ khả dụng khi ≤ trần học sinh" kiểm bằng `countActiveStudents(classroomId) <= classroom.maxStudents`. Link lớp tự nối vào cuối nội dung qua `resolveTenantOrigin` (helper có sẵn, dùng trong `announcement-email.handler.ts`) + `/classrooms/:id`. `sendEmail: false` có chủ đích — đây là thông báo trong ứng dụng (xuất hiện ở trang Notifications sẵn có qua `user_announcements`), không gửi email, thu hẹp phạm vi so với tên gọi "thông báo" gợi ý ban đầu.
- **Cron auto-archive 30 ngày**: `ClassroomCron` mới (`@Cron(CronExpression.EVERY_DAY_AT_MIDNIGHT)`), theo đúng mẫu `AssignmentsCron`/`CourseCron` — bọc `TenantDbRunnerService.runForEachTenant`. `ClassroomRepository.findInactiveClassroomIds(cutoffIso)` đọc `classrooms.viewed_at` (đã có sẵn cột + index `classrooms_viewed_at_idx` từ C1, chưa từng dùng tới trước C4) so với `now() - 30 ngày` (`CLASSROOM_AUTO_ARCHIVE_INACTIVE_DAYS`). **Dry-run mặc định**: cron luôn tính danh sách ứng viên và ghi log số lượng; chỉ thực sự `UPDATE archived_at` khi biến môi trường `CLASSROOM_AUTO_ARCHIVE_ENABLED=true`. Quyết định kỹ thuật: dùng **env var** thay vì cờ trong `settings` JSON (`liveTrainingEnabled`-style) như phác thảo ban đầu trong "Key Technical Context" — khảo sát trước khi code cho thấy không có tiền lệ "dry-run job" nào trong codebase, và mẫu cờ settings-JSON đòi hỏi sửa `DEFAULT_GLOBAL_SETTINGS`/schema/UI Settings admin (diện rộng hơn hẳn phạm vi C4); mẫu env-var-tắt-mặc-định đã có tiền lệ (`SLACK_OAUTH_ENABLED`) và đủ cho một cron nội bộ chưa cần cấu hình per-tenant. Lớp bị auto-archive có `archivedBy = null` (khác `archivedBy = <teacherId>` khi giáo viên tự archive) — phân biệt được nguồn gốc archive mà không cần cột mới.
- **Tự đăng ký giáo viên**: endpoint mới `POST /classroom/become-teacher`, **cố ý không có `@RequirePermission`** — đây chính là cách một user _chưa có_ `classroom.create` gọi được (guard toàn cục `PermissionsGuard` cho qua khi handler không khai yêu cầu quyền nào, chỉ cần đã đăng nhập). Khảo sát trước khi code xác nhận **không có tiền lệ self-service role-grant** nào trong codebase — `PATCH /users` (chỗ duy nhất đổi `roleSlugs`) chặn cứng bằng `USER_MANAGE`, kể cả khi user tự sửa chính mình. Quyết định: endpoint mới, riêng, gated bằng env var `CLASSROOM_TEACHER_SELF_REGISTRATION_ENABLED` (mặc định tắt — admin phải bật), cộng 2 lớp kiểm tra: đã có `classroom.create` thì từ chối (`classroom.error.alreadyTeacher`, chặn tự-đăng-ký lặp), và role `trainer` phải tồn tại cho tenant. Gán vai trò **cộng thêm** (không thay thế) qua `ClassroomRepository.addUserRole` (insert `permission_user_roles` với `onConflictDoNothing`, dựa vào unique index `(userId, roleId)` có sẵn) — khác hẳn `UserService.replaceUserRoleAssignments` (xóa hết rồi gán lại), vì tự-đăng-ký không được phép làm mất vai trò `student` sẵn có. **Giới hạn đã biết**: quyền embed trong JWT lúc đăng nhập (`PermissionsService.getUserAccess`) — sau khi tự đăng ký, quyền `classroom.create` chỉ có hiệu lực từ lần đăng nhập tiếp theo, đã xác minh đúng hành vi này qua HTTP thật (xem Test Evidence) và ghi rõ trong thông báo toast phía UI ("sign in again to activate it").
- **Trang quản trị**: endpoint mới `GET /classroom/admin/classrooms` (route 3 đoạn `admin/classrooms`, không đụng độ với route `:classroomId` 1 đoạn — đã kiểm tra kỹ theo đúng bài học route-ordering từ `PublicProfileController` được ghi trong C3), gated `PERMISSIONS.CLASSROOM_MANAGE` (admin-only, khác hẳn `CLASSROOM_READ` mọi route khác dùng — vì đây là oversight xuyên-tenant-role, không theo quan hệ giáo viên/học sinh). `ClassroomRepository.listAllClassroomsForAdmin` JOIN 2 subquery đếm (`teacherCount`/`activeStudentCount`) — không N+1. Frontend: `Admin/Classroom/AdminClassroomList.page.tsx` (bảng đơn giản dùng trực tiếp `Table`/`TableBody`/`TableRow` — **không** dùng `useReactTable` như `Groups.page.tsx`, vì đây là trang chỉ-đọc, không cần sort/bulk-select/pagination phức tạp), route `admin/classrooms` mới trong `routes.ts`, `routeAccessConfig["admin/classrooms"]` + `AdminGuard.isAllowed` trong `Admin.layout.tsx` đều thêm `CLASSROOM_MANAGE`. **Chưa thêm** vào menu điều hướng sidebar chính (không tìm thấy file cấu hình sidebar tập trung phù hợp trong thời gian đợt này) — thay vào đó có 1 nút "All classrooms (admin)" trên chính `ClassroomList.page.tsx` cho user có `CLASSROOM_MANAGE`, đủ để khám phá được tính năng; thêm vào sidebar chính để lại cho Follow-up Work.
- Không migration nào trong đợt này — mọi cột/bảng cần thiết (`wall`, `viewed_at`, `sourceType`/`sourceId` trên `announcements`, unique index trên `permission_user_roles`) đã tồn tại sẵn từ C1 hoặc từ hạ tầng announcements/RBAC có trước Classroom.
- 3 endpoint mới trong `classroom.controller.ts` (`sendClassroomAnnouncement`, `becomeClassroomTeacher`, `listAllClassroomsForAdmin`) — xác minh không trùng tên qua `no-duplicate-controller-method-names.spec.ts`.
- i18n: `classroom.wall.*`, `classroom.announcement.*` (gồm `.toast.*`), `classroom.becomeTeacher.*` (gồm `.toast.*`), 4 khóa `classroom.error.*` mới, namespace `adminClassrooms.*` mới (ngoài `classroom.*`, không nằm trong phạm vi `classroomLocaleKeyParity.spec.ts` nhưng vẫn dịch thật đủ 7 locale để nhất quán), khóa `pages.adminClassrooms` — dịch thật vào đủ 7 file trong chính đợt này.

### Đợt C5 (hoàn tất)

- **Khảo sát trước khi code thu hẹp phạm vi mục 6 của spec ("Giao bài tập (`assignments`) cho lớp")**: xác nhận `assignments` là con 1-1 của `lessons` (`assignments_lesson_id_unique_idx`), không có cột `groupId`/`classroomId` nào và cũng không có tiền lệ "giao bài tập cho group" tách rời khỏi "gán khóa học cho group" trong toàn bộ codebase — `assignments.repository.ts` có comment tường minh giải thích việc join qua course/enrollment thay vì `group_courses` chính vì lý do này. Kết luận: mục "giao bài tập cho lớp" **không cần code riêng** — bài tập tự động truy cập được ngay khi khóa học chứa nó được gán cho lớp (mục dưới). Không viết thêm gì cho mục này.
- **Gán/gỡ khóa học cho lớp**: bảng mới `classroom_courses` (mirror `group_courses`, migration `0207_add_classroom_courses` generated + `0208_enable_classroom_courses_rls` custom) + cột mới `student_courses.enrolled_by_classroom_id` (mirror `enrolled_by_group_id`, cùng migration `0207`) — **có chủ đích không thêm** cột `calendarEventId`/tích hợp nhắc hạn qua lịch như `group_courses` có (`GroupCourseDueDateCalendarService`), vì spec C5 không nhắc tới lịch nhắc hạn cho lớp, và tích hợp đó sẽ mở rộng phạm vi đáng kể. `ClassroomRepository.assignCourse`/`unassignCourse` **sao chép sát cấu trúc giao dịch** của `CourseService.enrollGroupsToCourse`/`unenrollGroupsFromCourse` (2 lượt: gắn provenance cho học sinh đã tự ghi danh trước đó rồi mới ghi danh học sinh mới, và khi gỡ — gắn lại provenance sang lớp khác nếu học sinh còn ở lớp khác cũng gán khóa học đó, nếu không thì soft-unenroll giữ nguyên tiến độ) — **triển khai như một đường code riêng, độc lập, không refactor `CourseService`** để giữ đúng nguyên tắc rủi ro thấp đã áp dụng xuyên suốt C1–C4 (không đụng vào luồng `groups` đang chạy tốt). `ClassroomModule` cần `imports: [..., forwardRef(() => CourseModule)]` (đúng mẫu `GroupModule` đã dùng) để gọi `CourseService.createCourseDependencies` (method public sẵn có, tạo tiến độ chương/bài cho học sinh mới) sau khi ghi danh. **Cổng bảo mật cố ý giữ nguyên ngưỡng của `groups`**: giáo viên lớp (`assertCanManage`) **chưa đủ** để gán khóa học — còn cần `PERMISSIONS.COURSE_ENROLLMENT` hoặc là tác giả khóa học (`canUpdateCourseByAuthor`), y hệt điều kiện `enrollGroupsToCourse` đã áp dụng cho `groups` từ trước — tránh một giáo viên bất kỳ ghi danh miễn phí cả lớp vào khóa học trả phí của người khác. Vai trò `trainer` mặc định **không có** `COURSE_ENROLLMENT`/`COURSE_UPDATE` (xác nhận trong `packages/shared/src/constants/permissions.ts`) nên tính năng này chủ yếu dùng được bởi `admin` hoặc giáo viên đồng thời là tác giả khóa học — hạn chế đã biết, chấp nhận được vì giữ đúng bằng bảo mật hiện có thay vì hạ thấp.
- **Chia sẻ Study cờ vua cho lớp**: **không thêm bảng/cột mới** — `ClassroomService.assignStudy` chỉ lặp qua toàn bộ học sinh đang hoạt động của lớp, gọi `ChessStudyService.addMember` (method public sẵn có từ trước, tự kiểm tra quyền sở hữu Study qua `assertCanManage` nội bộ của module chess) cho từng em — tái dùng 100% cơ chế `chess_study_members` (chia sẻ theo từng user) đã có, không viết lại. Xử lý khéo trường hợp biên: nếu một học sinh trong lớp trùng chính là tác giả Study, `addMember` ném `BadRequestException` — bọc try/catch bỏ qua đúng học sinh đó (không hủy cả thao tác hàng loạt), còn lỗi khác (403/404) vẫn ném ngay từ lượt lặp đầu tiên vì điều kiện đó giống nhau cho mọi học sinh. `ClassroomModule` import thẳng `ChessModule` (đã export `ChessStudyService`) — không phát sinh circular-dependency (đã xác nhận bằng `nest start` thật).
- **Mời hàng loạt vào giải đấu cờ, không auto-join**: cột mới `chess_tournaments.classroom_id` (nullable, mirror `group_id` — cùng migration `0207`/index cùng kiểu). **Không thêm endpoint nào trong `classroom.controller.ts`** — mở rộng thẳng `createTournamentBodySchema`/`ChessTournamentService.createTournament` để nhận thêm `classroomId` tùy chọn song song `groupId` (một tournament có thể set 1, cả 2, hoặc không cái nào). `assertEligible` thêm nhánh kiểm tra `classroomId` y hệt logic `groupId` sẵn có (`getClassroomStudentIds` mới trong `ChessTournamentRepository`, mirror `getGroupMemberIds`) — với SWISS/ARENA giữ đúng ngữ nghĩa "đủ điều kiện tham gia, không tự động thêm" mà `groupId` vốn đã có (chỉ SIMUL mới auto-invite, và đã mở rộng logic đó để ưu tiên `participantUserIds` tường minh → `groupId` → `classroomId`). Đây là lựa chọn tận dụng tối đa: không cần bảng "invite" riêng nào, vì `groupId`-based eligibility-only-scoping của tournament SWISS/ARENA vốn đã đúng là "mời hàng loạt, không auto-join" — chỉ cần nhân bản cho `classroomId`. Form tạo giải đấu (`ChessTournamentCreate.page.tsx`) thêm 1 `Select` "Classroom (optional)" độc lập cạnh `Select` group hiện có, dùng `useTeachingClassrooms()` sẵn có — tab Bulk pairing **giữ nguyên chỉ dùng group** (không mở rộng, vì `bulkPairingBodySchema` không nằm trong phạm vi đợt này).
- **Bug thật phát hiện qua HTTP thật, không unit test nào bắt được** (mock repository không chạy SQL thật): `classroomCourses.dueDate` khai báo `timestamp(..., { withTimezone: true })` **không có `mode: "string"`** → Drizzle trả về `Date` object thay vì string, khiến response `classroomCourseListSchema` (kỳ vọng `Type.String()`) validation-fail sau khi giao dịch DB đã **commit thành công** (do interceptor RLS bọc toàn bộ request trong 1 transaction theo tenant — response-validation lỗi ở tầng interceptor phía sau khiến toàn bộ request rollback, kể cả phần ghi dữ liệu đã "xong" trước đó) — hậu quả là ghi danh coi như không xảy ra dù DB tạm thời có dữ liệu đúng. Sửa bằng đúng mẫu có sẵn cho `groupCourses.dueDate` trong `course.service.ts`/`group.service.ts`: `sql<string | null>`TO_CHAR(${classroomCourses.dueDate}, 'YYYY-MM-DD"T"HH24:MI:SS"Z"')``— không sửa kiểu cột (giữ`Date`nội bộ, chỉ ép kiểu lúc SELECT ra API, đúng quy ước đã có trong module`courses`).
- Migration `0207_add_classroom_courses` (generated: bảng `classroom_courses` + 2 cột `chess_tournaments.classroom_id`/`student_courses.enrolled_by_classroom_id`) + `0208_enable_classroom_courses_rls` (custom, theo đúng mẫu RLS `0206`).
- 4 endpoint mới trong `classroom.controller.ts` (`listClassroomCourses`, `assignClassroomCourse`, `unassignClassroomCourse`, `assignClassroomStudy`) — xác minh không trùng tên qua `no-duplicate-controller-method-names.spec.ts`.
- i18n: `classroom.courses.*` (gồm `.toast.*`), `classroom.studies.*` (gồm `.toast.*`), 3 khóa `classroom.error.*` mới — dịch thật đủ 7 locale. `chessTournament.create.classroom`/`.selectClassroom` **chỉ thêm vào en+vi** — xác nhận namespace `chessTournament.*` vốn đã là nợ kỹ thuật có từ trước (chỉ tồn tại ở en+vi, đúng quy ước đã ghi nhận ở Đợt 11 L-series), giữ nguyên hiện trạng, không backfill toàn bộ namespace ngoài phạm vi C5.

### Đợt C6 (hoàn tất)

- **`GET /classroom/:classroomId/progress?days=N`** mới — teacher-facing (`assertCanManage`, không phải `assertCanRead` như `listCourses`): báo cáo tiến độ lộ rating/win-rate/tiến độ khóa học của **từng** học sinh, không chỉ của chính mình, nên chốt ngưỡng ngang với các thao tác quản lý khác (sửa lớp, xem ghi chú riêng học sinh) thay vì ngang mức "đọc" mà học sinh cùng lớp cũng được phép. Route đặt cạnh `:classroomId/students`/`:classroomId/courses` hiện có trong `classroom.controller.ts`, cùng `@RequirePermission(CLASSROOM_READ)` thô + service tự siết bằng `assertCanManage`.
- **Không có bảng mới, không migration** — toàn bộ dữ liệu đọc từ bảng đã tồn tại (`chess_ratings`, `chess_rating_history`, `chess_matches`, `chess_puzzle_attempts`, `chess_play_sessions`, `chess_learn_progress`, `student_courses`, `classroom_courses`). `ClassroomRepository` có 7 query mới (mirror gần đúng các query đã có sẵn trong `chess-class.repository.ts#getClassProgress`, nhưng viết lại riêng trong module `classroom` thay vì import chéo — module `chess-class` đang trên đường bị xóa hẳn ở C8, không nên tạo phụ thuộc mới vào nó).
- **Thu hẹp phạm vi có chủ đích so với gạch đầu dòng gốc của mục 5** ("% hoàn thành Learn/Practice/Coordinate"): khảo sát trước khi code cho thấy 3 nhánh nội dung nhập môn không đồng nhất — **Learn** có chương trình cố định (`CHESS_LEARN_STAGES`, 11 cấp độ toàn cục) nên tính % hoàn thành toàn cục là tự nhiên; **Practice** (`chess_practice_attempts`) gắn theo từng chương của một Study cụ thể, và `assignStudy` (C5) **không lưu lại** Study nào đã được gán cho lớp (chỉ thêm thành viên vào `chess_study_members`, đúng quyết định "không thêm bảng mới" đã ghi ở C5) — không có cách xác định "Study của lớp này" để tính %; **Coordinate** (`ChessCoordinateTrainer.page.tsx`) **không có bảng lưu tiến độ nào ở backend** (xác nhận qua khảo sát `apps/api/src/chess`), không có dữ liệu để báo cáo. Kết luận: C6 chỉ trả về **Learn** (`learnCompletedLevels`/`learnTotalLevels`/`learnCompletionPercentage`, tích lũy toàn thời gian — không lọc theo `days`, vì một cấp độ chỉ hoàn thành một lần); Practice/Coordinate **không có trong response**, ghi nhận rõ ở Follow-up Work thay vì trả số liệu rỗng gây hiểu lầm là "đã tính nhưng bằng 0".
- **`rating đầu/cuối`** hiện thực bằng cách lấy dòng `chess_rating_history` đầu tiên/cuối cùng theo `category` trong khoảng `days` (mảng đã `ORDER BY created_at ASC` sẵn ở tầng repository); nếu học sinh không có dòng lịch sử nào trong khoảng đó (không thi đấu/không đổi rating), cả `ratingStart` và `ratingEnd` fallback về `current` (rating hiện tại từ `chess_ratings`) thay vì `0`/`null` — tránh biểu đồ tụt xuống 0 giả tạo cho học sinh không hoạt động trong kỳ báo cáo.
- **`thời lượng`** chỉ tính từ `chess_play_sessions.duration_ms` (chơi với máy) — **có chủ đích không tính** thời lượng ván đấu người-với-người (`chess_matches`), vì bảng đó không có cột lưu thời lượng thật (chỉ có `last_move_at`, suy ra thời lượng sẽ sai nếu người chơi treo máy giữa ván) — ghi rõ trong docstring của `getPlayDurationForUsers`.
- **Tiến độ khóa học LMS**: query mới `getClassroomCourseProgress` — LEFT JOIN từ **toàn bộ học sinh đang hoạt động của lớp** (không phải chỉ học sinh có `enrolledByClassroomId = classroomId`) sang `student_courses`, để phân biệt đúng 3 trạng thái: `not_enrolled` (chưa từng ghi danh — kể cả khi giáo viên đã gán khóa học cho lớp), `not_started`/`in_progress`/`completed` (đã ghi danh, theo cột `progress` có sẵn). Quyết định kỹ thuật: dùng roster của lớp làm nguồn chính thay vì lọc theo cột provenance `enrolled_by_classroom_id`, vì học sinh có thể tự ghi danh khóa học đó từ trước khi lớp gán, hoặc provenance có thể bị gán lại sang lớp khác khi gỡ khóa học (hành vi đã có từ C5) — dùng roster tránh báo cáo bỏ sót học sinh trong 2 trường hợp biên đó. `completionPercentage` = `finished_chapter_count / courses.chapter_count` (cột đếm sẵn có, không JOIN đếm lại `chapters`), làm tròn số nguyên.
- Không thêm gì cho "bài tập" (`assignments`) trong mục 3 của Feature Functions — đúng kết luận đã ghi ở C5 (bài tập không có khái niệm gán riêng cho lớp/group, tự động truy cập được qua khóa học chứa nó).

### Đợt C7 (hoàn tất)

- **Khảo sát trước khi code** xác nhận "giáo viên chỉ thấy học sinh lớp mình dạy" và "học sinh chỉ thấy bạn cùng lớp" (2/3 vế của mục 7) **đã đúng từ C1/C3** một cách cấu trúc: `listStudents`/`getStudentDetail` luôn nhận `classroomId` cụ thể và gate qua `assertCanRead`/`assertCanManage` (chỉ giáo viên/học sinh **của đúng lớp đó**, người lạ nhận 404) — không có endpoint nào từng trả `realName` xuyên nhiều lớp. Vế còn lại — "**chính chủ không thấy**" tên thật của chính mình — chưa từng có: `listClassroomStudents` trả `realName` như nhau cho mọi hàng, kể cả hàng của chính người gọi. `ClassroomService` thêm `maskOwnRealName` (helper riêng, không phải "một serializer" tách module như câu chữ gốc gợi ý — 2 điểm gọi duy nhất trong cùng service không đủ lý do tách file) — so `row.userId` với `user.userId`, trả `realName: null` cho đúng 1 hàng đó. Áp dụng ở `listStudents`/`getStudentDetail`; **không** áp dụng cho các endpoint chỉ giáo viên dùng để _sửa_ học sinh (`updateStudent`, `setClassroomStudentArchived`, …) vì việc giáo viên tự sửa hồ sơ của chính mình-như-học-sinh là trường hợp biên gần như không xảy ra và những endpoint đó không phải nơi "xem" tên thật của người khác.
- **Đổi kiểu API có chủ đích**: `classroomStudentSchema.realName` từ `Type.String()` sang `Type.Union([Type.String(), Type.Null()])` — chọn `null` tường minh thay vì chuỗi rỗng (`""`) làm sentinel, vì `realName` không bao giờ hợp lệ là rỗng (validate `minLength: 1` lúc tạo), nên `""` sẽ mơ hồ với lỗi dữ liệu thật trong tương lai, còn `null` thì không. Ảnh hưởng 4 điểm hiển thị ở frontend (`ClassroomStudentsList`, `ClassroomBulkActions`, `ClassroomStudentDetail`, `ClassroomProgress` — trang mới từ C6) — tất cả fallback về khóa i18n mới `classroom.students.you` ("Bạn"/"You"...) khi `realName === null`.
- **Kid-mode DM/follow theo `canMsg` + giáo viên**: khảo sát trước khi code phát hiện `CommunitySocialRepository.sharesClassmateRelationship` (đổi tên thành `canInteractAsManagedAccount` cho đúng ngữ nghĩa mới — không còn thuần "classmate") đã tồn tại từ C1 (UNION `group_users`/`classroom_students`) nhưng **chưa từng đọc cột `classrooms.can_msg`** — một lớp tắt nhắn tin (`canMsg: false`) vẫn cho phép 2 học sinh managed nhắn nhau qua đường `classroom_students`. Thêm điều kiện `AND c.can_msg = true` vào nhánh classroom của UNION (nhánh `groups` giữ nguyên không gate — `groups` chưa từng có khái niệm `canMsg`, không suy diễn thêm quyền hạn chế ngoài phạm vi). Đồng thời phát hiện **học sinh managed không nhắn được với chính giáo viên của mình** (không nằm trong `classroom_students`×`classroom_students`) — mâu thuẫn với câu chữ spec "hoặc với giáo viên của mình" — thêm nhánh thứ 3 vào UNION: cặp giáo viên-học sinh cùng lớp, **luôn cho phép bất kể `canMsg`** (đường thoát tới người lớn không nên bị khóa cùng lúc với việc khóa nhắn-tin-giữa-học-sinh). `listClassmates` (danh sách "có thể nhắn tin cho ai" hiển thị cho tài khoản managed) có cùng lỗi lệch: chỉ đọc `group_users`, **thiếu hẳn nhánh classroom** dù `assertCanInteract` đã cho phép — sửa bằng UNION 3 nhánh y hệt, giữ nguyên điều kiện loại trừ người bị chặn (`community_user_relationships`).
- **Không tự đổi mật khẩu khi bị quản lý**: `UserService.changePassword` chưa từng kiểm tra `isManagedAccount` — thêm guard ngay sau khi xác nhận user tồn tại, ném `ForbiddenException("changePasswordView.validation.managedAccountForbidden")`. 2/3 hạn chế còn lại của mục 7 ("không tự tắt kid mode", "không tự đóng tài khoản") **đã đúng từ trước** — xác nhận qua khảo sát: không có endpoint nào cho phép user tự sửa `isManagedAccount`/tự xóa chính mình (`PATCH`/`DELETE /users` đều yêu cầu `USER_MANAGE`, một quyền không role tự-quản-lý nào có).
- **GDPR ghost hoá**: `GdprService.anonymizeUser`/`UserService.deleteUser`/`UserService.deleteBulkUsers` đều chỉ `UPDATE` (soft-scrub) bảng `users`, không bao giờ `DELETE` — nên `ON DELETE CASCADE` sẵn có trên `classroom_teachers.user_id`/`classroom_students.user_id`/`classroom_invites.user_id` (từ migration `0201`/`0205`) **không bao giờ chạy**, để lại `realName`/`notes` (PII độc lập với bảng `users`, giáo viên tự nhập) tồn tại vĩnh viễn sau khi "xóa" tài khoản. Thêm hàm dùng chung `ghostClassroomMembership(trx, userId)` (`src/classroom/utils/ghost-classroom-membership.ts`, hàm thuần không phải service — tránh phải nối `ClassroomModule` vào `UserModule qua` DI chỉ để xóa 3 dòng) — `DELETE` (không phải scrub-tại-chỗ) cả 3 bảng theo `userId`, gọi trong transaction sẵn có của cả 3 luồng xóa/ẩn danh. Quyết định kỹ thuật: xóa hẳn `classroom_teachers`/`classroom_invites` thay vì giữ lại bản ghi "ghost" — không có ý nghĩa giữ lại một giáo viên đã bị xóa tài khoản trong danh sách giáo viên của lớp; không áp dụng lại giới hạn "không xóa giáo viên cuối cùng của lớp" (giới hạn đó chỉ có ý nghĩa cho thao tác tự nguyện `removeTeacher`, không phải cho việc tài khoản bị xóa vĩnh viễn) — lớp có thể tạm thời còn 0 giáo viên sau đó, chấp nhận được như một biên hiếm gặp, ghi ở Follow-up Work. **Không** mở rộng sang `groups`/`group_users` dù có lỗ hổng y hệt — ngoài phạm vi mục 7 (chỉ nói "vai trò trong lớp"), giữ đúng nguyên tắc rủi ro thấp không đụng vào `groups` xuyên suốt roadmap.

## Test Evidence

### Đợt C1

- Backend: `pnpm --filter=api exec tsc --noEmit` sạch; eslint sạch trên toàn bộ file mới/sửa; `pnpm --filter=api test` — 80/80 test suite, 627/627 test pass (bao gồm `classroom.service.spec.ts` — 15 test case cho `assertCanRead`/`assertCanManage`/trần giáo viên/guard giáo viên cuối cùng; `no-duplicate-controller-method-names.spec.ts`; `community-social.service.spec.ts` sau khi đổi tên `shareGroup`→`sharesClassmateRelationship`).
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch; eslint sạch; `pnpm --filter=web test` — 51/51 test suite, 264/264 test pass (bao gồm `classroomLocaleKeyParity.spec.ts` — 7/7).
- **Xác minh qua HTTP thật** (Postgres + Redis thật qua Docker, API chạy `nest start` thật, đăng nhập bằng 2 tài khoản trainer đã seed sẵn của `tenant1.lms.localhost`): tạo lớp → xem chi tiết (xác nhận `viewedAt` cập nhật) → sửa tên → thêm giáo viên thứ 2 → giáo viên đầu tự rời lớp → xác nhận giáo viên đã rời nhận **404** khi xem/sửa lớp (không phải 403 — đúng quy tắc "không lộ tồn tại") → giáo viên còn lại thử tự xóa mình (giáo viên cuối cùng) → nhận đúng lỗi `classroom.error.lastTeacher` (400) → archive → reopen — toàn bộ đúng như kỳ vọng. Dữ liệu test đã dọn sau khi xác minh xong.
- **Xác minh riêng cho điểm rủi ro cao nhất của kế hoạch** (sửa `shareGroup`): chạy trực tiếp câu SQL `UNION ALL` mới trong một transaction `ROLLBACK` (không ghi dữ liệu thật) trên Postgres thật — xác nhận trả về 1 dòng khi 2 user cùng là thành viên **đang hoạt động** của một `classroom`, và trả về 0 dòng khi một trong hai đã bị archive khỏi lớp (`archived_at IS NOT NULL`) — đúng ngữ nghĩa "chỉ tính bạn cùng lớp đang học".
- `pnpm --filter=api db:migrate` chạy thật trên Postgres dev (Docker), áp dụng `0201`/`0202` thành công, không lỗi.
- `pnpm generate:client` chạy thật (API dev server sống, sinh `api-schema.json` qua Swagger, sinh `generated-api.ts` qua `swagger-typescript-api`) — xác nhận 8 method `classroomController*` được đặt tên đúng, không ghi đè type của method nào khác.

**Giới hạn đã biết**: chưa kiểm tra qua trình duyệt thật (Playwright/thao tác chuột) trong đợt này — chỉ xác minh UI qua `tsc`/eslint/Vitest sạch và luồng API thật qua HTTP trực tiếp (không qua Caddy, vì máy dev không có domain `*.lms.localhost` trỏ Caddy sẵn sàng trong phiên này — gọi thẳng `localhost:3000` kèm header `x-forwarded-host`/`x-forwarded-proto` giả lập đúng tenant). Chưa kiểm tra lại luồng kid-mode DM qua tầng API/service đầy đủ với tài khoản managed thật (managed account chỉ tạo được từ Đợt C3 trở đi) — chỉ xác minh câu SQL cốt lõi trực tiếp trên DB thật; sẽ kiểm tra lại toàn luồng khi C3 có endpoint tạo học sinh managed.

### Đợt C2

- Backend: `pnpm --filter=api exec tsc --noEmit` sạch; eslint sạch; `pnpm --filter=api test` — 80/80 test suite, 627/627 test pass (không hồi quy so với C1; `chess-class.service.spec.ts` vẫn xanh nguyên vẹn — xác nhận thêm `@ApiOperation` không đổi hành vi runtime).
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch; eslint sạch; `pnpm --filter=web test` — 51/51 test suite, 264/264 test pass.
- `pnpm --filter=api db:migrate` chạy thật trên Postgres dev (Docker), áp dụng `0203`/`0204` thành công. Xác nhận trên DB dev thật: 0 dòng backfill (đúng — dev DB hiện chưa có tài khoản managed nào, `is_managed_account = true` đếm được 0 dòng, không phải lỗi).
- **Xác minh logic backfill với dữ liệu thật** (transaction `ROLLBACK`, không ghi dữ liệu thật): tạo 1 group test + 2 user managed (cùng `managed_by_user_id`) + 2 dòng `group_users`, chạy nguyên văn 3 câu lệnh của migration `0204` → xác nhận đúng 1 `classrooms` (tên lấy đúng từ `groups.name->>'en'`, `owner_id` đúng giáo viên), đúng 1 `classroom_teachers`, đúng 2 `classroom_students` (tên thật lấy đúng từ `users.real_name`) — khớp 100% kỳ vọng.
- `pnpm generate:client` chạy lại thật sau khi thêm endpoint cầu nối — xác nhận `apps/api/src/swagger/api-schema.json` có đúng 5 endpoint `deprecated: true`; `classroomControllerGetClassroomIdForSourceGroup` sinh đúng tên, không trùng.
- Smoke test HTTP thật: `GET /api/classroom/by-source-group/:groupId` với group không tồn tại trong bảng backfill → trả `{ classroomId: null }`, HTTP 200 (không lỗi) — đúng hành vi mong đợi cho trang cũ khi chưa có lớp tương ứng.

### Đợt C3

- Backend: `pnpm --filter=api exec tsc --noEmit` sạch; `pnpm --filter=api lint` sạch; `pnpm --filter=api test` — **80/80 test suite, 640/640 test pass** (tăng từ 627 ở C2; bao gồm `classroom.service.spec.ts` mở rộng lên 26 test case — thêm 13 test mới cho `createStudent`/trần 100 học sinh, `resetStudentPassword`/`releaseStudent` chỉ áp dụng tài khoản managed, 4 nhánh phản hồi của `inviteStudent`, luồng accept/decline + "đổi ý" sau khi đã decline).
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch (kể cả sau khi regenerate `generated-api.ts` từ schema mới); `pnpm --filter=web lint` sạch; `pnpm --filter=web test` — **51/51 test suite (2 skip không liên quan), 264/264 test pass (12 skip không liên quan)**, bao gồm `classroomLocaleKeyParity.spec.ts` 7/7.
- `pnpm --filter=api db:migrate` chạy thật trên Postgres dev (Docker), áp dụng `0205`/`0206` thành công, không lỗi.
- **Xác minh qua HTTP thật** (API `nest start` sống, Postgres/Redis thật qua Docker, header `x-forwarded-host: tenant1.lms.localhost` giả lập tenant vì môi trường dev không có Caddy): tạo lớp mới → tạo 1 học sinh managed đơn lẻ (xác nhận mật khẩu chỉ trả về đúng 1 lần trong response `create`) → tạo hàng loạt 3 học sinh từ danh sách tên → mời 1 tài khoản trainer có sẵn (sau khi set `username` thủ công qua SQL vì tài khoản seed gốc không có username — hành vi từ chối `userNotFound` trước đó là đúng, không phải bug) → nhận lời mời thành công → chuyển 1 học sinh sang lớp khác (xác nhận `notes` và `archivedAt` giữ nguyên qua kiểm tra byte-level bằng `octet_length`) → reset mật khẩu học sinh managed → archive rồi khôi phục 1 học sinh → thao tác hàng loạt archive 2 học sinh cùng lúc.
- Trong lúc xác minh phát hiện và sửa 2 lỗi thật (không bắt được bởi unit test vì mock repository không chạy SQL thật): (1) `PATCH .../students/:userId` và `.../archive` trả HTTP 400 do response thiếu `username` — service giờ gọi lại `getClassroomStudent()` (JOIN đủ cột) sau khi `UPDATE` thay vì dùng trực tiếp row trả về từ `UPDATE...RETURNING`; (2) `moveStudent` không giữ `notes`/`archivedAt` khi chuyển lớp — `insertClassroomStudent()` nay nhận tham số `preserve` tùy chọn.
- `pnpm generate:client` chạy lại thật với API dev server sống — xác nhận `api-schema.json` có đủ 22 route `/api/classroom*` (8 từ C1/C2 + 17 mới, trừ 3 route trùng do có 2 method HTTP trên cùng 1 path), `generated-api.ts` sinh đúng 17 hàm `classroomController*` mới, không ghi đè type của method nào khác.
- **Môi trường**: bộ `pnpm --filter=api test` ban đầu chạy dưới Node 25.2.1 (mặc định trên PATH của máy) làm 16/80 suite crash khi import (lỗi `Cannot read properties of undefined (reading 'prototype')` tại `buffer-equal-constant-time`, đúng như đã ghi nhận từ trước — Node 25 phá `jsonwebtoken`/`SlowBuffer`) — không liên quan tới thay đổi của C3 (0 test case thất bại, chỉ suite-load crash trên các file không liên quan tới `classroom`). Tải Node 22.15.0 dạng portable, chạy lại sạch 100%.

**Giới hạn đã biết**: vẫn chưa kiểm tra qua trình duyệt thật/Playwright trong đợt này (hoãn đúng như kế hoạch tới C8) — 5 trang UI mới đã qua `tsc`/eslint/Vitest sạch và luồng API thật đã xác minh qua HTTP trực tiếp, nhưng chưa thao tác chuột thật trên 5 trang đó. Chưa xác minh lại toàn luồng kid-mode DM giữa 2 học sinh managed cùng lớp qua tầng API đầy đủ (chỉ xác minh câu SQL cốt lõi ở C1) — nay đã có thể tạo tài khoản managed thật, việc này nên làm ở đầu C4 trước khi thêm tính năng bảng tin.

### Đợt C4

- Backend: `pnpm --filter=api exec tsc --noEmit` sạch; `pnpm --filter=api lint` sạch; `pnpm --filter=api test` — **80/80 test suite, 652/652 test pass** (tăng từ 640 ở C3; `classroom.service.spec.ts` mở rộng lên 38 test case — thêm 12 test mới cho `wall` qua `updateClassroom`, `sendAnnouncement` (forbidden/quá trần/tạo đúng announcement với link nối vào content), `autoArchiveInactiveClassrooms` (0 ứng viên/dry-run/enabled), `becomeTeacher` (flag tắt/đã là giáo viên/gán vai trò cộng thêm), `listAllClassroomsForAdmin`).
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch (kể cả sau khi regenerate `generated-api.ts`); `pnpm --filter=web lint` sạch trên toàn bộ file mới/sửa; `pnpm --filter=web test` — **51/51 test suite (2 skip không liên quan), 264/264 test pass (12 skip không liên quan)**, bao gồm `classroomLocaleKeyParity.spec.ts` 7/7; `pnpm --filter=web exec remix vite:build` build production thành công (client + SSR).
- **Hạ tầng xác minh**: môi trường lần này không có Docker daemon khả dụng ban đầu (registry pull bị mạng chặn) — chuyển sang Postgres 16 + Redis cài đặt native (đã có sẵn trên máy), cài thêm gói `postgresql-16-pgvector` (thiếu so với image `pgvector/pgvector:pg16` dùng trong `docker-compose.yml`) để `db:migrate` chạy hết toàn bộ 210 migration (bao gồm các bảng AI/RAG dùng cột `vector`) mà không lỗi. `db:seed` chạy thật, tạo đúng vai trò `lms_app_user` (RLS runtime role) + 3 tenant dữ liệu mẫu.
- **Xác minh qua HTTP thật** (API `nest start` sống, Postgres/Redis thật, header `Origin: https://tenant3.lms.localhost` giả lập tenant — môi trường không có Caddy, dùng thẳng `localhost:3000` với `Origin` thay vì `Host`/`x-forwarded-host` như C1–C3 vì tenant resolver đọc CORS Origin cho request trực tiếp):
  - Tạo lớp mới (`trainer+tenant3@example.com`, mật khẩu reset thủ công qua SQL để đăng nhập được) → `PATCH .../wall` với nội dung Markdown → xác nhận response `wall` cập nhật đúng.
  - `POST .../announcements` với message 5 ký tự → **400** đúng lỗi `minLength` như schema quy định; gửi lại với message hợp lệ khi lớp chỉ có 1 giáo viên (chính người gửi) và 0 học sinh → **201**, kiểm tra trực tiếp bảng `announcements` thấy đúng 1 dòng `source_type='classroom'` với `content` đã nối link `https://tenant3.lms.localhost/classrooms/:id`, nhưng `user_announcements` **0 dòng** (đúng — người nhận duy nhất bị loại vì chính là người gửi). Tạo thêm 1 học sinh managed trong lớp rồi gửi lại → `user_announcements` xuất hiện đúng 1 dòng cho học sinh đó (giáo viên/người gửi vẫn bị loại) — xác nhận đúng ngữ nghĩa "giáo viên ∪ học sinh đang hoạt động, trừ người gửi".
  - `GET /classroom/admin/classrooms` với tài khoản `trainer` (không có `CLASSROOM_MANAGE`) → **403** `auth.error.missingPermission`; với tài khoản `admin+tenant3@example.com` → **200**, trả đúng lớp vừa tạo với `teacherCount: 1`, `activeStudentCount: 1`.
  - `POST /classroom/become-teacher`: gọi bằng tài khoản trainer (đã có `classroom.create`) khi biến môi trường `CLASSROOM_TEACHER_SELF_REGISTRATION_ENABLED` **chưa bật** → **400** `classroom.error.selfRegistrationDisabled` (đúng — mặc định tắt). Khởi động lại API với biến môi trường bật, gọi bằng tài khoản `student2+tenant3@example.com` (chỉ có vai trò `student`) → **201**, kiểm tra `permission_user_roles` thấy user có cả `student` **và** `trainer` (không mất vai trò cũ). Đăng nhập lại (JWT mới) rồi gọi lại lần 2 → **400** `classroom.error.alreadyTeacher` (đúng — JWT mới đã có `classroom.create`, guard idempotency hoạt động đúng).
  - Toàn bộ dữ liệu test (lớp, học sinh managed, vai trò `trainer` gán thêm cho `student2`) đã dọn sau khi xác minh xong.
- `pnpm generate:client` chạy lại thật với API dev server sống — xác nhận `api-schema.json` có đủ 3 route mới (`POST /classroom/{classroomId}/announcements`, `POST /classroom/become-teacher`, `GET /classroom/admin/classrooms`), `generated-api.ts` sinh đúng 3 hàm `classroomController*` mới, không ghi đè type của method nào khác — diff chỉ 77 dòng, không đụng tới method nào khác.

**Giới hạn đã biết**: vẫn chưa kiểm tra qua trình duyệt thật/Playwright (hoãn tới C8 như các đợt trước). Vẫn chưa xác minh lại toàn luồng kid-mode DM giữa 2 học sinh managed cùng lớp qua tầng API đầy đủ — dời tiếp sang một đợt sau (không phải trọng tâm của C4, và C4 tự nó đã đủ diện tích). Trang quản trị mới chưa có lối vào từ menu điều hướng sidebar chính (chỉ có nút trên `ClassroomList.page.tsx` + truy cập trực tiếp URL `/admin/classrooms`). Cơ chế dry-run/bật-tắt của cron auto-archive và tự đăng ký giáo viên dùng biến môi trường (đọc lúc khởi động lại server), không phải cờ per-tenant chỉnh được ngay từ UI Settings như `liveTrainingEnabled` — chấp nhận được ở quy mô hiện tại (đơn tenant/CLB), nhưng sẽ cần nâng cấp lên cờ trong `settings` JSON nếu triển khai đa tenant cần bật/tắt độc lập theo từng trường/CLB.

### Đợt C5

- Backend: `pnpm --filter=api exec tsc --noEmit` sạch; `pnpm --filter=api lint` sạch; `pnpm --filter=api test` — **80/80 test suite, 669/669 test pass** (tăng từ 652 ở C4; `classroom.service.spec.ts` mở rộng lên 68 test case — 18 test mới cho `assignCourse`/`unassignCourse`/`listCourses`/`assignStudy`; `chess-tournament.service.spec.ts` mở rộng lên 27 test case — 5 test mới cho `classroomId` trong `createTournament`/`joinTournament`, bao gồm xác nhận SWISS không auto-add player khi scoped theo classroom).
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch (kể cả sau khi regenerate `generated-api.ts`); `pnpm --filter=web lint` sạch trên toàn bộ file mới/sửa; `pnpm --filter=web test` — **51/51 test suite (2 skip không liên quan), 264/264 test pass (12 skip không liên quan)**; `remix vite:build` build production thành công.
- `pnpm --filter=api db:migrate` chạy thật trên Postgres 16 native (không Docker, lý do như C4), áp dụng `0207`/`0208` thành công, không lỗi. Xác nhận trực tiếp `\d classroom_courses`/`\d student_courses`/`\d chess_tournaments` — cột/FK/index/RLS đúng như migration.
- **Xác minh qua HTTP thật** (API `nest start` sống, Postgres/Redis native, `Origin: https://tenant3.lms.localhost`):
  - Trainer (không có `COURSE_ENROLLMENT`, không phải tác giả khóa học) gọi `POST .../courses` → **403** `classroom.error.courseAssignNotAllowed` — đúng cổng bảo mật.
  - Admin (có `COURSE_ENROLLMENT`) gán 1 khóa học thật cho lớp có 1 học sinh managed → **phát hiện bug thật** (đã sửa, xem Key Technical Context) → sau khi sửa: **201**, `student_courses` có đúng 1 dòng `status='enrolled'`, `enrolled_by_classroom_id` đúng bằng lớp, và **4 dòng `student_chapter_progress`** được tạo tự động qua `createCourseDependencies` — xác nhận luồng ghi danh đầy đủ, không chỉ tạo liên kết suông.
  - Gỡ khóa học (`DELETE .../courses/:courseId`) → **200**, `student_courses` giữ nguyên dòng nhưng `status` chuyển `not_enrolled`, `enrolled_by_classroom_id` về `NULL`, `finished_chapter_count` giữ nguyên `0` (đúng — soft-unenroll, không xóa lịch sử tiến độ). Gọi gỡ lần 2 → **404** `classroom.error.courseNotAssigned`.
  - Tạo 1 Study cờ vua thật (tài khoản admin), gán cho lớp qua `POST .../studies` → **201** `{studentCount: 1}`, `chess_study_members` có đúng 1 dòng `role='read'` cho học sinh managed trong lớp.
  - Tạo 1 giải đấu SWISS gán `classroomId` (không set `groupId`) → **201**, xác nhận `chess_tournament_players` **0 dòng** ngay sau khi tạo (đúng — không auto-join). Học sinh **ngoài** lớp (`student2`) gọi `POST .../join` → **403** `chess.tournament.errors.notInClassroom`. Học sinh managed **trong** lớp gọi `POST .../join` → **201**, `chess_tournament_players` có đúng 1 dòng — đúng ngữ nghĩa "đủ điều kiện tham gia, tự nguyện join, không auto-add".
  - Toàn bộ dữ liệu test (lớp, học sinh managed, khóa học ghi danh, Study, giải đấu) đã dọn sau khi xác minh xong (bao gồm dọn các bảng phụ thuộc `student_chapter_progress`/`student_lesson_progress`/`activity_logs` trước khi xóa user managed, do ràng buộc khóa ngoại).
- `pnpm generate:client` chạy lại thật với API dev server sống — xác nhận `api-schema.json` có đủ 3 route mới dưới `/classroom` (`GET/POST .../courses`, `DELETE .../courses/:courseId`, `POST .../studies`) cộng `classroomId` mới trong `CreateTournamentBody`/`chessTournamentSchema`; `generated-api.ts` sinh đúng 4 hàm `classroomController*` mới, diff 131 dòng, không ghi đè type của method nào khác.

**Giới hạn đã biết**: vẫn chưa kiểm tra qua trình duyệt thật/Playwright (hoãn tới C8 như các đợt trước). Chưa xác minh lại kid-mode DM (dời tiếp từ C3/C4, không phải trọng tâm C5). "Giao bài tập cho lớp" không có UI/endpoint riêng theo đúng phân tích trong Key Technical Context — nếu sau này phát sinh nhu cầu thật (ví dụ: giao một bài tập cụ thể mà không giao cả khóa học chứa nó), đó sẽ là một tính năng mới hoàn toàn, không phải phần còn thiếu của C5. Form tạo giải đấu chỉ hỗ trợ chọn lớp cho tab Swiss/Arena/Simul, không hỗ trợ Bulk pairing (giữ nguyên phạm vi `bulkPairingBodySchema` chỉ theo `groups`).

### Đợt C6

- Backend: `pnpm --filter=api exec tsc --noEmit` sạch (28 lỗi tiền-tồn tại không liên quan `classroom` — xác nhận giống hệt trên baseline trước khi sửa, do lệch phiên bản `tsc` cài toàn cục trong môi trường build, không phải do C6); `pnpm --filter=api lint` sạch trên `src/classroom`; `pnpm --filter=api test` — **80/80 test suite, 673/673 test pass** (tăng từ 669 ở C5; `classroom.service.spec.ts` mở rộng lên 54 test case — 4 test mới cho `getProgressReport`: 403 khi học sinh gọi, 404 cho người lạ, tính đúng win rate/puzzle accuracy/rating đầu-cuối/trung bình lớp, và phân biệt đúng `not_enrolled` với `0%` cho học sinh chưa từng ghi danh khóa học đã gán cho lớp).
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch (0 lỗi); `pnpm --filter=web eslint` sạch trên file mới/sửa; `pnpm --filter=web exec vitest run` — **51/51 test suite (2 skip không liên quan), 264/264 test pass (12 skip không liên quan)** — không tăng so với C5 vì C6 không thêm unit test riêng cho trang mới (theo đúng tiền lệ: các trang Classroom trước đó cũng không có test component riêng, chỉ có `classroomLocaleKeyParity.spec.ts` xuyên suốt, đã chạy lại xanh với 7 khóa `classroom.progress.*` mới); `remix vite:build` build production thành công.
- **Xác minh qua HTTP thật** (API `nest start` sống, Postgres 16 + Redis native trong container phiên này — cài `postgresql-16-pgvector` vì migration cần extension `vector`, tạo role `lms_app_user` qua `SEED_MANAGE_DB_ROLE=true`, chạy `db:migrate` rồi `db:seed`, header `Origin: https://tenant3.lms.localhost`):
  - Tạo lớp mới + 2 học sinh managed qua API thật → `GET .../progress?days=30` → **200**, cả 2 học sinh xuất hiện với toàn bộ số liệu chess bằng 0 (đúng — chưa có hoạt động cờ), `learnTotalLevels: 11` (đúng tổng số cấp độ `CHESS_LEARN_STAGES` toàn cục), `courses: []` (đúng — chưa gán khóa học nào).
  - Gán 1 khóa học thật (4 chương) cho lớp qua `POST .../courses` → gọi lại `GET .../progress` → **200**, cả 2 học sinh xuất hiện trong `courses[0].students` với `progress: "not_started"`, `finishedChapterCount: 0`, `completionPercentage: 0`, `totalChapterCount: 4`, `enrolledCount: 2` — xác nhận đúng luồng ghi danh tự động qua `assignCourse` (C5) được `getClassroomCourseProgress` (C6) đọc lại chính xác.
  - Phát hiện lỗi trong kịch bản test (không phải lỗi sản phẩm): lần thử đầu tiên chọn nhầm một `courseId` thuộc tenant khác (`SELECT ... FROM courses LIMIT 3` không lọc tenant) → RLS đúng đắn trả về danh sách rỗng cho cả `assignCourse` lẫn `listClassroomCourses` thay vì lộ dữ liệu tenant khác hay lỗi 500 — xác nhận RLS hoạt động đúng ở biên này. Chọn lại `courseId` đúng tenant → hành vi đúng như mô tả ở trên.
  - Toàn bộ dữ liệu test (lớp, học sinh managed, khóa học ghi danh) đã dọn sau khi xác minh xong.
- `pnpm generate:client` chạy lại thật với API dev server sống (không hand-edit) — xác nhận `api-schema.json` có route mới `GET /classroom/{classroomId}/progress`; `generated-api.ts` sinh đúng 1 hàm `classroomControllerGetClassroomProgress` + type `GetClassroomProgressResponse`, diff 78 dòng, không ghi đè type của method nào khác.

**Giới hạn đã biết**: vẫn chưa kiểm tra qua trình duyệt thật/Playwright (hoãn tới C8 như các đợt trước). Practice/Coordinate không có trong báo cáo tiến độ (xem lý do trong Key Technical Context) — nếu sau này Study được gán cho lớp có lưu lại liên kết `classroom_id` (thay đổi thiết kế `assignStudy` của C5), có thể bổ sung % Practice theo đúng Study đó. Chưa có UI lọc/sắp xếp bảng học sinh khi lớp đông (bảng hiện render toàn bộ danh sách, chưa phân trang) — chấp nhận được ở quy mô một lớp học (tối đa 100 học sinh theo `CLASSROOM_DEFAULTS.MAX_STUDENTS`), nhưng nên cân nhắc lại nếu trần đó tăng lên. Rating "đầu/cuối" chỉ tính theo `chess_rating_history` — không tính lại từ `chess_matches`/`chess_puzzle_attempts` thô, nên nếu có sai lệch giữa 2 nguồn đó (không nên xảy ra vì mọi lần cập nhật rating đều ghi cả `chess_ratings` lẫn `chess_rating_history` cùng lúc, xem `chess-puzzle.repository.ts`), báo cáo sẽ theo `chess_rating_history`.

### Đợt C7

- Backend: `pnpm --filter=api exec tsc --noEmit` — **0 lỗi** (28 lỗi tiền-tồn tại ghi nhận ở C6 nay tự hết sau khi `pnpm --filter="@repo/*" build` build lại `@repo/shared`/`@repo/prompts`/`@repo/email-templates` trong phiên này — xác nhận đó đúng là vấn đề môi trường/thứ tự build, không phải lỗi code); `pnpm --filter=api lint` sạch trên toàn bộ file sửa; `pnpm --filter=api test` — **80/80 test suite, 677/677 test pass** (tăng từ 673 ở C6) — `classroom.service.spec.ts` mở rộng lên 57 test case (3 test mới cho `maskOwnRealName` qua `listStudents`/`getStudentDetail`); `community-social.service.spec.ts` đổi tên mock theo `canInteractAsManagedAccount` (không tăng test case, hành vi cũ vẫn đúng qua tên mới); `gdpr.service.spec.ts` thêm 1 test xác nhận `ghostClassroomMembership` được gọi đúng 3 bảng trong transaction anonymize.
- Frontend: `pnpm --filter=web exec tsc --noEmit` sạch (0 lỗi, kể cả sau khi regenerate `generated-api.ts` với `realName: string | null`); `pnpm --filter=web eslint` sạch trên file mới/sửa; `pnpm --filter=web exec vitest run` — **51/51 test suite (2 skip không liên quan), 264/264 test pass (12 skip không liên quan)**, bao gồm `classroomLocaleKeyParity.spec.ts` chạy lại xanh với khóa `classroom.students.you` mới; `remix vite:build` build production thành công.
- **Xác minh qua HTTP thật** (API `nest start` sống, Postgres 16 + Redis native dựng lại trong container phiên này, header `Origin: https://tenant3.lms.localhost`):
  - Tạo lớp `canMsg: false` + 2 học sinh managed → đăng nhập bằng email nội bộ (không phải `username` — `AuthService.validateUser` chỉ tra theo `email`, bài học ghi lại cho lần sau) + mật khẩu tạm → `GET .../students` với tư cách học sinh 1: hàng của chính học sinh 1 có `realName: null`, hàng học sinh 2 vẫn `"Student Two C7c"` — đúng "chính chủ không thấy, bạn cùng lớp vẫn thấy".
  - Học sinh 1 gọi `POST /community/messages` tới học sinh 2 khi `canMsg: false` → **403** `communityView.errors.kidModeNotClassmates` (đúng — gate mới hoạt động). Cùng học sinh 1 gọi tới **giáo viên chủ nhiệm lớp** (vẫn khi `canMsg: false`) → **201** (đúng — nhánh giáo viên không bị `canMsg` chặn). `PATCH .../classroom` bật `canMsg: true` → gọi lại tới học sinh 2 → **201** (đúng — gate mở khi bật cờ). `GET /community/messageable-users` với tư cách học sinh 1 → danh sách chứa đúng cả giáo viên lẫn học sinh 2 (đúng — `listClassmates` đã vá đồng bộ với `assertCanInteract`).
  - Học sinh 1 gọi `PATCH /user/change-password?id=<chính họ>` với mật khẩu tạm đúng → **403** `changePasswordView.validation.managedAccountForbidden` (route đúng là `/api/user/...` số ít, không phải `/api/users/...` — một lần gọi nhầm route ra 404 trong lúc test, không phải lỗi sản phẩm).
  - `POST /user/gdpr-anonymize?id=<học sinh 1>` (tài khoản admin) → **201**; truy vấn trực tiếp `SELECT COUNT(*) FROM classroom_students WHERE user_id = ...` → **0 dòng** (trước đó là 1) — xác nhận `ghostClassroomMembership` chạy đúng trong transaction thật, không chỉ trong test có mock.
  - Toàn bộ dữ liệu test (lớp, học sinh managed, hội thoại community) đã dọn sau khi xác minh xong.
- `pnpm generate:client` chạy lại thật với API dev server sống — xác nhận `api-schema.json` cập nhật `realName` từ `string` sang `string | null` ở đúng 6 response type liên quan tới `classroom_students` (`ListClassroomStudentsResponse`, `GetClassroomStudentDetailResponse`, `UpdateClassroomStudentResponse`, `SetClassroomStudentArchivedResponse`, `MoveClassroomStudentResponse`, `RunClassroomBulkActionResponse`), không có route mới (C7 không thêm/xóa endpoint nào) — diff 12 dòng.

**Giới hạn đã biết**: vẫn chưa kiểm tra qua trình duyệt thật/Playwright (hoãn tới C8 như các đợt trước). `maskOwnRealName` không áp dụng cho các endpoint chỉ-giáo-viên dùng để sửa học sinh (`updateStudent`, bulk actions, …) — biên "giáo viên tự sửa hồ sơ của chính mình-như-học-sinh" gần như không xảy ra trong thực tế, chấp nhận được, xem lý do trong Key Technical Context; `ClassroomEditStudent.page.tsx` sẽ hiện ô "Tên thật" trống thay vì tên thật của giáo viên trong đúng biên đó. Ghosting GDPR (C7) chỉ xử lý bảng của module `classroom` (`classroom_teachers`/`classroom_students`/`classroom_invites`) — **không** mở rộng sang lỗ hổng y hệt ở `groups`/`group_users` (ngoài phạm vi mục 7, xem Key Technical Context). Không có bảo vệ "không xóa giáo viên cuối cùng" khi ghosting qua GDPR (khác với thao tác tự nguyện `removeTeacher`) — một lớp có thể tạm thời còn 0 giáo viên sau khi giáo viên duy nhất của lớp bị xóa tài khoản.

## Follow-up Work (explicitly not done in this pass)

- C8 chưa triển khai tại thời điểm C7 kết thúc.
- Ghosting GDPR chưa mở rộng sang `groups`/`group_users` (lỗ hổng tương tự, ngoài phạm vi mục 7 — xem Key Technical Context Đợt C7).
- Chưa có bảo vệ "lớp còn 0 giáo viên" sau khi GDPR-xóa giáo viên duy nhất của lớp — xem Giới hạn đã biết Đợt C7.
- `maskOwnRealName` chưa xử lý biên "giáo viên tự sửa hồ sơ chính mình-như-học-sinh" trong các endpoint sửa (`updateStudent`, bulk actions) — xem Giới hạn đã biết Đợt C7.
- Báo cáo tiến độ (C6) không có % Practice/Coordinate — xem lý do và điều kiện để bổ sung sau này trong mục "Đợt C6" ở Key Technical Context.
- Báo cáo tiến độ (C6) chưa có nút xuất CSV/PDF hay xem theo từng học sinh riêng lẻ (chỉ có bảng tổng hợp toàn lớp) — nếu giáo viên cần chia sẻ báo cáo ra ngoài hệ thống, đây sẽ là phần mở rộng.
- Kiểm tra UI bằng trình duyệt thật/Playwright — hoãn tới C8 (đợt E2E).
- Backfill data thật (không phải test data rolled-back) chưa từng chạy trên một dev DB có tài khoản managed thật — sẽ xác nhận lại khi tenant nào đó thực sự có dữ liệu L5 cũ trong quá trình vận hành.
- Namespace `chessClass.*` vẫn thiếu hoàn toàn ở `pl`/`de`/`lt`/`cs`/`es` (nợ kỹ thuật có từ Đợt L5) — không backfill trong đợt này vì ngoài phạm vi. Namespace `chessTournament.*` cùng tình trạng (chỉ en+vi), C5 giữ nguyên hiện trạng thay vì mở rộng.
- Trang quản trị `admin/classrooms` chưa có mục trong menu điều hướng sidebar chính — chỉ truy cập được qua nút trên trang danh sách lớp học hoặc URL trực tiếp.
- Cờ `CLASSROOM_AUTO_ARCHIVE_ENABLED`/`CLASSROOM_TEACHER_SELF_REGISTRATION_ENABLED` là biến môi trường toàn cục (mọi tenant cùng bật/tắt), không phải cờ per-tenant trong `settings` — nâng cấp lên cờ per-tenant nếu cần kiểm soát độc lập theo tenant trong tương lai.
- Gán khóa học cho lớp chưa có tích hợp nhắc hạn qua lịch (`calendarEventId`) như `group_courses` đã có — nếu cần, đó là một bổ sung nhỏ tái dùng `GroupCourseDueDateCalendarService`.
- Trần "COURSE_ENROLLMENT hoặc tác giả khóa học" khiến giáo viên (`trainer`) thường không tự gán được khóa học cho lớp mình dạy (vì mặc định không có 2 quyền đó) — đây là đánh đổi bảo mật có chủ đích (xem Key Technical Context), nhưng nếu trải nghiệm thực tế cho thấy quá hạn chế, có thể cân nhắc thêm một quyền hẹp hơn kiểu `COURSE_ENROLLMENT_OWN_CLASSROOM` trong tương lai thay vì hạ thấp ngưỡng chung.
