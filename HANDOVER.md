# HANDOVER — covuahocduong.com (Chess on Mentingo)

**Ngày:** 2026-07-26 (cập nhật)
**Nền tảng:** monorepo Mentingo LMS + module cờ vua (MIT-only)
**Domain mục tiêu:** `covuahocduong.com`
**Trạng thái:** W1–W4 (module cờ) + đợt phát triển LMS-core PR1–PR5 (xem mục 0 dưới) đều đã **merged vào `main`**.

---

## -2. Cập nhật mới nhất (2026-07-26) — Đợt "còn thiếu so với LearnHouse", tự động triển khai liên tục

Sau khi khảo sát lại **toàn bộ** LearnHouse (backend `apps/api` ~30 router/53 bảng, frontend `apps/web`/`apps/collab`/`apps/cli`/`apps/e2e`, trừ `ee/`) và đối chiếu kỹ với mã nguồn mentingo hiện tại, đã cập nhật lại `docs/research/learnhouse/03-feature-matrix.md` + `05-roadmap.md` với danh sách 10 đợt còn lại (kế hoạch chi tiết đã duyệt, lưu tại `C:\Users\duongsinh\.claude\plans\sau-khi-kh-o-s-t-cozy-wave.md` phía máy dev). Theo yêu cầu của user, mỗi đợt tự động verify rồi commit + push + tạo PR + merge vào `main`, không dừng lại hỏi giữa chừng.

| Đợt             | PR               | Nội dung                                                                                                                                                                                                                                                                                                                                                        |
| --------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Đợt 1** (#9)  | merged           | 5 block editor mới (`callout,flipcard,badge,buttonLink,webPreview`), menu `/` slash-command + nút "+" toolbar dùng chung registry, gạch chân/căn lề, endpoint `GET /api/link-preview` có SSRF guard 2 lớp + cache Redis. Spec: `docs/specs/rich-text-blocks-business-spec.md`.                                                                                  |
| **Đợt 2** (#10) | merged           | Hoàn thiện Assignment engine: wire 3 field đã có schema/API nhưng chưa lên UI (hint, reference file, anti-copy-paste), reject bài nộp, summary chấm điểm, cron nhắc hạn nộp, trang chấm nâng cấp (search/filter/sort/quick-grade). Spec: addendum trong `docs/specs/assignment-engine-business-spec.md`.                                                        |
| **Đợt 3** (#11) | merged           | Bảo mật tài khoản: khóa đăng nhập tạm thời sau N lần sai (ngưỡng/thời lượng cấu hình được), captcha Cloudflare Turnstile tùy chọn cho `/auth/register` + `/auth/login` (tự tắt khi thiếu env, không breaking change). Đo độ mạnh mật khẩu đã có sẵn từ trước — không cần làm gì thêm. Spec: `docs/specs/account-security-hardening-business-spec.md`.           |
| **Đợt 4** (#12) | merged           | Lịch sử phiên bản nội dung bài học: snapshot title/description mỗi lần lưu bài học loại content (tối đa 20 bản, xóa bản cũ nhất khi vượt), panel xem lịch sử + xem trước read-only + khôi phục, cảnh báo xung đột chỉnh sửa đồng thời (optimistic locking qua `updatedAt`). Spec: `docs/specs/lesson-content-versioning-business-spec.md`.                      |
| **Đợt 5** (#13) | merged           | Analytics sâu phần 2 (org-level): DAU trend, mới-vs-quay-lại, cohort retention theo tuần, hoạt động theo ngày trong tuần (thay heatmap giờ — không khả thi), phân bố điểm quiz/bài tập, tỉ lệ cấp chứng chỉ, điểm gắn kết 3 thành phần, xuất XLSX nâng cao. Spec: `docs/specs/deep-analytics-phase2-business-spec.md`.                                          |
| **Đợt 6** (#14) | merged           | AI đợt 2 (thu hẹp phạm vi còn "sinh quiz bằng AI"): sinh câu hỏi trắc nghiệm từ nội dung 1 bài học, đề xuất rồi tác giả xác nhận qua form quiz sẵn có, nhật ký `ai_generations` + hạn mức 20 lượt/tháng/tenant. Sinh bài tập + Ask-AI RAG chat hoãn sang Follow-up (lý do kiến trúc, xem mục dưới). Spec: `docs/specs/ai-quiz-generation-business-spec.md`.     |
| **Đợt 7** (#15) | merged           | Diễn đàn cộng đồng (thu hẹp phạm vi) + hồ sơ công khai `/u/:username`: bài đăng/bình luận 1 cấp/upvote, kiểm duyệt (từ chặn + giới hạn bài/ngày), username + bio opt-in công khai. Reaction emoji/slow-mode/tự khóa/yêu cầu email xác minh hoãn sang Follow-up. Spec: `docs/specs/community-forum-business-spec.md`.                                            |
| **Đợt 8** (#16) | merged           | Thư viện tài nguyên phân cấp (thu hẹp phạm vi — chỉ phần thư mục, bàn cờ cộng tác hoãn đợt riêng): thư mục lồng nhau (màu/ảnh bìa/thứ tự), gán tài nguyên vào thư mục, chặn vòng lặp cha-con, chỉ xóa thư mục rỗng, lưới thư mục + breadcrumb trong dialog thư viện tài nguyên sẵn có. Spec: `docs/specs/hierarchical-resource-library-business-spec.md`.       |
| **Đợt 9** (#17) | merged           | Webhook đi ra (thu hẹp phạm vi — 6 sự kiện cốt lõi thay vì ~40, không làm tích hợp Zapier riêng): endpoint webhook admin quản lý (URL/sự kiện/bật-tắt/secret mã hoá), ký HMAC-SHA256, gửi qua hàng đợi BullMQ với retry backoff, SSRF guard tái dùng từ link-preview, nút gửi thử + log 50 lần gần nhất. Spec: `docs/specs/outbound-webhooks-business-spec.md`. |
| **Đợt 10**      | merged           | GDPR export + ẩn danh hóa (thu hẹp phạm vi — chỉ hạng mục này trong 8 hạng mục "vận hành & tuân thủ" ban đầu): xuất JSON toàn bộ dữ liệu 1 user (hồ sơ + học tập + cộng đồng), ẩn danh hóa tài khoản (xóa PII + thu hồi token, giữ nguyên chứng chỉ/thống kê), chặn ẩn danh hóa admin cuối cùng của tenant. Spec: `docs/specs/gdpr-user-data-business-spec.md`. |
| **Đợt 11**      | _(xem mục dưới)_ | Bàn phân tích cờ cộng tác thời gian thực (hoãn từ Đợt 8, triển khai như đợt riêng): phòng phân tích nhiều người cùng xem/đi nước qua `WsGateway`/Redis sẵn có, validate nước đi lại phía server bằng `chess.js`, host điều khiển reset FEN/kết thúc phòng. Spec: `docs/specs/collaborative-chess-analysis-board-business-spec.md`.                              |

**Đợt 2 — chi tiết:** khảo sát trước khi code phát hiện `assignment_tasks.hint`, `assignment_tasks.referenceFileS3Key`, `assignments.antiCopyPaste` đã tồn tại đầy đủ trong schema + TypeBox API contract nhưng **chưa từng được đưa lên UI** (authoring form không có ô nhập, learner view không hiển thị) — thu hẹp phạm vi đợt 2 thành "wire lên" 3 field đó thay vì coi là việc mới. Ngoài ra thêm: `PATCH /assignments/task-submissions/:id/reject` (reset submission, không đụng `attemptNumber`), `GET /assignments/:id/summary` (status breakdown/điểm TB/tỉ lệ đạt), cron `AssignmentsCron` (9h sáng hàng ngày, theo đúng pattern `CourseCron`/`CourseDueDateReminderEmailHandler` — outbox event → handler riêng gửi mail + tạo announcement). **Lưu ý kỹ thuật quan trọng**: `packages/email-templates/src/index.ts` là **file auto-generated** bởi `packages/email-templates/plugin.mjs` (chạy lúc `tsup build`) — sửa tay sẽ bị ghi đè ở lần build sau; phải sửa `plugin.mjs` rồi build lại. Tương tự, mọi lần thêm/sửa endpoint API cần **build lại `@repo/shared` và `@repo/email-templates`** (`pnpm --filter=@repo/shared build && pnpm --filter=@repo/email-templates build`) trước khi `tsc` của `apps/api` thấy được type mới, và cần **khởi động tạm API bằng Node 22** rồi `pnpm run generate:client` để đồng bộ `apps/web/app/api/generated-api.ts` sau mỗi lần đổi contract (xem mục rủi ro Node 25 bên dưới — export schema chạy trước `app.listen()` nên vẫn lấy được file dù cổng 3000 bị chiếm).

Task còn deferred ở Đợt 2 (ghi rõ trong spec, không phải thiếu sót): 2 loại task mới (quiz trắc nghiệm điểm từng phần, form điền chỗ trống) và autosave bài làm học viên — cả hai đủ lớn để tách đợt riêng nếu cần.

**Đợt 3 — chi tiết:** thêm 2 cột `failedLoginAttempts`/`lockedUntil` trên `users` (migration `0170_add_login_lockout_fields.sql`); `AuthService.validateUser` kiểm tra khóa trước khi so mật khẩu, tăng bộ đếm khi sai, khóa khi chạm ngưỡng (`maxFailedLoginAttempts`/`lockoutMinutes` mới trong `globalSettingsJSONSchema`, mặc định 5 lần / 15 phút), reset khi đăng nhập thành công — **luôn trả về đúng một thông báo lỗi chung** dù là sai mật khẩu hay đang bị khóa, để không lộ thông tin tài khoản. Turnstile theo đúng mẫu "config tùy chọn tự tắt" của Google/Slack/Microsoft OAuth (`apps/api/src/common/configuration/turnstile.ts` + `hasTurnstileConfig`), `TurnstileGuard` mới áp cho route `register`/`login`, site key phơi qua `GET /api/env/frontend/turnstile` (`@Public()`), frontend có `useTurnstileSiteKey()` + `TurnstileWidget` (tải script Cloudflare động, chỉ khi có site key). Test mới: `auth.service.lockout.spec.ts` (6 test) + `turnstile.guard.spec.ts` (5 test), cả hai chạy Jest với **Node 22** (Node 25 vỡ do `buffer-equal-constant-time`/`jsonwebtoken`, xem mục rủi ro Node bên dưới).

**Đợt 4 — chi tiết:** bảng mới `lesson_content_versions` (migration `0171_add_lesson_content_versions.sql` + `0172_enable_lesson_content_versions_rls.sql`, theo đúng mẫu RLS `chess_exercises`/`0163`) lưu `lessonId`, `language`, `versionNumber` (tăng dần theo `MAX+1` trong cùng bảng), `title`/`description` dạng `text` (không phải jsonb — snapshot giá trị đã localize cho đúng 1 ngôn ngữ tại thời điểm lưu), `createdBy`. Hook vào `AdminLessonService.updateLesson`: **trước khi ghi đè**, nếu lesson là loại `content` và `description` mới khác `description` cũ, snapshot nội dung **cũ** (không phải mới) rồi prune về tối đa 20 bản. Khôi phục (`restoreLessonContentVersion`) tái sử dụng chính `updateLesson` với `forceOverwrite: true` — nên bản thân việc khôi phục cũng tự động được snapshot lại (khôi phục nhầm vẫn hoàn tác được). Xung đột chỉnh sửa đồng thời: `updateLesson` nhận thêm `expectedUpdatedAt`/`forceOverwrite`, so khớp với `lessons.updatedAt` hiện tại trong DB, ném `ConflictException` (409) nếu khác và không force — frontend bắt lỗi 409 và hỏi xác nhận lưu đè qua `window.confirm`. 3 endpoint mới đều dùng lại đúng permission gate (`COURSE_UPDATE`/`COURSE_UPDATE_OWN`) và `validateAccess` sẵn có của `updateLesson` — không thêm permission mới. Test mới: `lessonContentVersions.repository.spec.ts` (5 test, Jest Node 22).

**Đợt 5 — chi tiết:** khảo sát trước khi code phát hiện `user_statistics.activityHistory` (jsonb `{"yyyy-MM-dd": true}`, dùng cho tính năng streak sẵn có) đã đủ dữ liệu để tính DAU trend/mới-vs-quay-lại/hoạt động theo ngày trong tuần **mà không cần pipeline sự kiện mới** — nhưng **không đủ** để làm bản đồ nhiệt theo giờ (chỉ có độ phân giải theo ngày), nên mục đó bị loại khỏi phạm vi và thay bằng "hoạt động theo ngày trong tuần". Truy vấn mới dùng `db.execute(sql\`...\`)`với`jsonb_each_text`/`generate_series`/`date_trunc('week', ...)`(theo đúng pattern raw-SQL sẵn có trong`statistics.repository.ts`) vì Drizzle query builder không hỗ trợ trực tiếp "mở" object jsonb thành hàng. 7 endpoint mới `GET /api/analytics/org/\*` (`AnalyticsRepository`/`AnalyticsService`mở rộng, controller mới`OrgAnalyticsController`— tách khỏi`AnalyticsController`hiện có vì controller đó dùng`AnalyticsSecretGuard`cho endpoint`active-users`công khai, không phù hợp cho các endpoint admin-UI cần permission`STATISTICS_READ`). Xuất XLSX nâng cao dùng `write-excel-file` nhiều sheet (`objects: Object[][]`+`schema: Schema[]`+`sheets: string[]`) ngay trong `AnalyticsService`, không phụ thuộc `ReportModule`. Test mới: `analytics.service.spec.ts` (8 test, Jest Node 22).

**Đợt 6 — chi tiết:** khảo sát trước khi code phát hiện phạm vi ban đầu (sinh quiz + sinh bài tập + Ask-AI RAG chat trên mọi bài học) quá rộng cho 1 đợt: pipeline RAG hiện tại (`rag.repository.ts`) **gắn cứng vào bảng `ai_mentor_lessons`** qua `document_to_ai_mentor_lesson` — mở rộng RAG ra mọi bài học cần tổng quát hóa lược đồ liên kết tài liệu, một quyết định kiến trúc riêng; và **không có tiền lệ reserve/deduct/refund** nào trong toàn bộ codebase để soi theo cho hạn mức AI. Quyết định: **thu hẹp Đợt 6 chỉ còn sinh quiz bằng AI**, hoãn sinh bài tập + Ask-AI sang Follow-up Work (ghi rõ trong spec). Kỹ thuật: `ChatService.generateStructured<T>()` mới (tổng quát hóa từ `ChatService.judge()` sẵn có, dùng `generateObject` của Vercel `ai` SDK với schema TypeBox tùy ý) — tái dùng nguyên vẹn hạ tầng Luma/core-fallback đã có, không gọi OpenAI trực tiếp. Bảng mới `ai_generations` (migration `0173`/`0174`, mẫu RLS `chess_exercises`) chỉ ghi nhật ký kiểm toán + đếm hạn mức — **không** dùng giao dịch giữ chỗ phức tạp, chỉ đếm-trước-rồi-làm (check-then-act, cùng dạng `assertMaxParallelSessionsAvailable` của live-training). Luồng "đề xuất rồi xác nhận": AI trả về câu hỏi nháp, frontend `form.setValue("questions", [...])` điền thẳng vào form Quiz Lesson **đã có sẵn** — không có đường ghi DB mới nào cho nội dung câu hỏi, tác giả vẫn bấm nút Lưu hiện có để lưu thật. Gate quyền: tái dùng `PERMISSIONS.COURSE_AI_GENERATION` đã có (đang dùng cho Luma course generation), không thêm quyền mới. Test mới: `quiz-generation.service.spec.ts` (7 test, Jest Node 22).

**Đợt 7 — chi tiết:** khảo sát trước khi code phát hiện phạm vi ban đầu (bài đăng + bình luận + upvote + **reaction emoji** + kiểm duyệt 10 tham số bao gồm slow-mode/tự-khóa/yêu-cầu-email-xác-minh) quá rộng — đặc biệt **hệ thống chưa có bất kỳ cơ chế xác minh email nào** (không có cột `emailVerified`), nên "yêu cầu email xác minh mới được đăng" không thể làm nếu không xây cả tính năng xác minh email từ đầu. Quyết định: thu hẹp còn bài đăng + bình luận 1 cấp + upvote (bỏ reaction emoji), 2 tham số kiểm duyệt (từ chặn + số bài tối đa/ngày, đếm theo cửa sổ trượt 24h chứ không phải ngày dương lịch), hồ sơ công khai cơ bản (username + avatar + bio, không tiến độ học tập). 3 bảng mới `community_posts`/`community_comments`/`community_votes` (migration `0175`/`0176`, mẫu RLS `chess_exercises`) + cột `username`/`publicProfileEnabled` trên `users`. **Lưu ý route quan trọng**: `PublicProfileController` có route wildcard `GET :username` — 2 route cụ thể `GET settings/me` và `PATCH settings` **phải khai báo trước** route wildcard trong class, nếu không NestJS sẽ khớp "settings" như một giá trị username. Route `/u/:username` đặt ngay dưới `NavigationWrapper` (không bọc trong route yêu cầu đăng nhập) để công khai thực sự; trả "không tìm thấy" chung cho cả username-không-tồn-tại lẫn publicProfileEnabled=false để chống dò username. Permission mới: `COMMUNITY_READ`/`COMMUNITY_POST_CREATE`/`COMMUNITY_POST_MANAGE_OWN`/`COMMUNITY_MODERATE`. Test mới: `community.service.spec.ts` (13 test) + `public-profile.service.spec.ts` (8 test), Jest Node 22.

**Đợt 8 — chi tiết:** khảo sát trước khi code xác nhận Đợt 8 ban đầu gộp 2 việc không liên quan kỹ thuật (thư viện phân cấp + bàn phân tích cờ cộng tác realtime) — thu hẹp chỉ làm phần thư viện phân cấp, bàn cờ cộng tác hoãn sang đợt riêng (kỹ thuật sketch đã ghi trong Follow-up Work của spec: room `WsGateway`/Redis adapter sẵn có, tái dùng `ChessBoard.tsx`, bảng `chess_analysis_sessions` mới). Bảng mới `resource_folders` (tự tham chiếu `parentFolderId` qua thunk `(): AnyPgColumn => resourceFolders.id`, màu cố định 8 lựa chọn, `coverResourceId` tham chiếu `resources.id`, `displayOrder`) + cột `folderId` nullable trên `resources` (migration `0177_add_resource_folders.sql` + `0178_enable_resource_folders_rls.sql`, mẫu RLS `chess_exercises`/`0163`). Chặn vòng lặp cha-con: `ResourceLibraryService.updateFolder` từ chối nếu `parentFolderId` mới trùng chính `id` hoặc nằm trong chuỗi tổ tiên của `parentFolderId` mới (duyệt ngược qua `getFolderAncestorChain`). Xóa thư mục: chỉ cho xóa khi không có thư mục con lẫn tài nguyên trực tiếp bên trong (`isFolderEmpty`). Mở rộng `resource-library.repository/service/controller.ts` hiện có (không tạo module mới) với endpoint `GET/POST/PATCH/DELETE /resource-library/folders` + `PATCH /resource-library/assets/:id/move`; `GET /assets` thêm filter `folderId` tùy chọn (giá trị đặc biệt `"root"` cho gốc thư viện, bỏ trống vẫn trả về toàn bộ như cũ — không phá vỡ nơi khác đang gọi). Vẫn dùng ké quyền `RESOURCE_LIBRARY_PERMISSIONS` hiện có, không thêm quyền `resource_library.*` riêng (nợ kỹ thuật đã có từ trước, ghi rõ trong Follow-up Work). Frontend: thư viện tài nguyên trong mentingo là **dialog nhúng trong toolbar RichText editor** (`AssetLibraryDialog.tsx`), không phải trang riêng — thêm lưới thư mục (`AssetLibraryFolderGrid`) + breadcrumb (`AssetLibraryBreadcrumb`) + popover tạo thư mục mới (`AssetLibraryNewFolderPopover`, dùng `Popover` thay vì lồng `Dialog` trong `Dialog`) + menu "Di chuyển đến..." trên từng tài nguyên (root + các thư mục ở cấp hiện tại) ngay trong dialog đó, giữ nguyên UI hiện có khi tìm kiếm toàn thư viện (tắt chế độ xem thư mục khi có từ khóa tìm kiếm). Test mới: `resource-library.service.folders.spec.ts` (13 test — chặn vòng lặp, xóa thư mục rỗng/không rỗng, di chuyển tài nguyên, Jest Node 22).

**Đợt 9 — chi tiết:** khảo sát trước khi code xác nhận hạ tầng outbox event (`apps/api/src/outbox/`) đã publish sẵn hầu hết sự kiện cần thiết (`EnrollCourseEvent`, `UserCourseFinishedEvent`, `LessonCompletedEvent`, `AssignmentGradedEvent`) — chỉ thiếu 2 sự kiện `assignment.submitted`/`certificate.issued` nên thêm `AssignmentSubmittedEvent` (publish trong `AssignmentsService.submitTask`, ngay sau `upsertTaskSubmission`) và `CertificateIssuedEvent` (publish trong `CertificatesService.createCertificate`, chỉ nhánh tạo mới — không phát lại khi gọi idempotent trả về chứng chỉ đã có). **Thu hẹp phạm vi**: 6 sự kiện cốt lõi thay vì registry ~40 sự kiện của hệ thống tham khảo, không làm tích hợp Zapier riêng (endpoint chuẩn POST JSON + HMAC đã tương thích thẳng với "Webhooks by Zapier"), không có thời gian chuyển tiếp khi tái sinh secret, không có nút gửi lại thủ công 1 lần gửi cụ thể (đã có retry tự động). Bảng mới `webhook_endpoints`/`webhook_deliveries` (migration `0179_add_webhooks.sql`/`0180_enable_webhooks_rls.sql`, mẫu RLS `chess_exercises`/`0163`) — secret mã hoá tại chỗ theo đúng kiểu envelope-encryption đã dùng trong `EnvService`/`IntegrationService` (KEK từ `MASTER_KEY`, DEK ngẫu nhiên mỗi secret), không viết lại logic mã hoá mới. `WebhookEventHandler` mới (`apps/api/src/webhooks/handlers/`) là **một** `@EventsHandler` lắng nghe cả 6 loại event có sẵn (không phải 6 handler riêng) — đọc `tenantId` trực tiếp từ `dbAls.getStore()` (AsyncLocalStorage) vì handler chạy trong đúng transaction tenant-scoped mà `OutboxDispatcherService` đã thiết lập. Gửi thật qua `WebhookDeliveryWorker` (BullMQ, mẫu `IngestionWorker`/`QUEUE_NAMES.WEBHOOK_DELIVERY`, `attempts: 3, backoff: exponential 1000ms`) — mỗi lần thử (kể cả retry) tự ghi 1 dòng `webhook_deliveries` vì worker throw lỗi để BullMQ tự retry toàn bộ job. SSRF guard: viết `apps/api/src/webhooks/utils/safe-webhook-post.ts` bằng cách nhân bản chính xác pattern `safeLookup`/re-check `remoteAddress` sau khi kết nối của `link-preview/utils/safe-fetch.ts` (đổi GET+đọc HTML thành POST+JSON, không theo redirect — endpoint webhook redirect bị coi là gửi thất bại thay vì được follow). Frontend: `WebhooksCard` mới trong Settings → Account (cùng vị trí + cùng điều kiện quyền `PERMISSIONS.INTEGRATION_KEY_MANAGE` với `IntegrationApiKeyCard` có sẵn — dùng ké quyền, không thêm quyền `webhook.*` riêng), form tạo endpoint (URL + checkbox 6 sự kiện), secret chỉ hiện 1 lần lúc tạo/tái sinh (mẫu reveal-once của `IntegrationApiKeyCard`), dialog xem log 50 lần gửi gần nhất. **Lưu ý test hiện có bị ảnh hưởng**: `assignments.service.spec.ts` có 1 test kỳ vọng `outbox.publish` gọi đúng 1 lần khi tự động chấm đạt GRADED — phải sửa thành 2 lần vì giờ `submitTask` luôn publish thêm `AssignmentSubmittedEvent` trước khi chấm. Test mới: `webhooks.service.spec.ts` (9 test, Jest Node 22).

**Đợt 10 — chi tiết:** khảo sát trước khi code xác nhận Đợt 10 gốc gộp 8 hạng mục "vận hành & tuân thủ" không liên quan kỹ thuật với nhau (GDPR, custom domain, menu builder, landing page builder, custom scripts, đồng tác giả khóa học, khóa bài học theo nhóm, xuất user CSV, SEO mặc định theo tenant — thực chất là 8 đợt tiềm năng). Thu hẹp: **chỉ làm GDPR export + ẩn danh hóa**, giá trị pháp lý cao nhất và độc lập kỹ thuật nhất; 7 hạng mục còn lại chuyển Follow-up Work, mỗi hạng mục đủ lớn để là 1 đợt riêng trong tương lai. Không cần bảng mới/migration — hoàn toàn dựa trên schema hiện có. `GdprService` mới (`apps/api/src/user/services/gdpr.service.ts`, mở rộng module `user` hiện có): `exportUserData` truy vấn song song ~13 bảng liên quan đến 1 user (hồ sơ, tiến độ học tập, quiz/bài tập, chứng chỉ, nội dung cộng đồng, thống kê), trả JSON thô; `anonymizeUser` mô phỏng chính xác mẫu ghi đè PII của `UsersService.deleteUser` hiện có (mở rộng thêm cột `avatarReference`/`username`/`publicProfileEnabled`, xóa thêm `resetTokens`/`createTokens`/`magicLinkTokens`/`credentials`) nhưng áp dụng được cho **mọi vai trò** (không giới hạn chỉ học viên như `deleteUser`), có thêm kiểm tra chặn ẩn danh hóa nếu đây là admin cuối cùng còn lại của tenant (đếm số admin khác qua `userHasPermissionCondition` — helper SQL có sẵn từ trước). **Chủ đích giữ nguyên** `certificates`/tiến độ học tập/`userStatistics`/thống kê tổng hợp cấp tenant để không phá vỡ báo cáo/đối soát — chỉ tên hiển thị đổi thành "Deleted User" (vì `authorId`/`userId` vẫn trỏ đúng, tên lấy từ `users.firstName`/`lastName` đã ghi đè). 2 endpoint mới trong `user.controller.ts` hiện có (không tạo controller riêng): `GET /user/gdpr-export?id=` (trả file JSON qua `res.send()`, mẫu `ReportController`), `POST /user/gdpr-anonymize?id=` — cả 2 dùng lại quyền `USER_MANAGE` hiện có, không thêm quyền `gdpr.*` riêng. Frontend: 2 nút trong khối "Danger zone" mới trên trang chi tiết người dùng admin hiện có (`apps/web/app/modules/Admin/Users/User.page.tsx`) — nút xuất tải file trực tiếp qua `fetch`+blob (mẫu `useDownloadSummaryReport`), nút ẩn danh hóa mở `AlertDialog` xác nhận (mẫu duy nhất dùng `AlertDialog` thật trong codebase, tại `EditCourse.tsx`). Test mới: `gdpr.service.spec.ts` (4 test, Jest Node 22) — mock `userHasPermissionCondition` thay vì dựng lại toàn bộ chuỗi `innerJoin` thật của nó, vì hàm này xây subquery `exists()` phức tạp không cần thiết phải mô phỏng đúng để kiểm tra logic nghiệp vụ của `GdprService`.

**Đợt 11 — chi tiết:** đợt riêng cho hạng mục đã hoãn từ Đợt 8 (bàn phân tích cờ cộng tác) — theo yêu cầu người dùng tiếp tục triển khai sau khi roadmap 10 đợt gốc hoàn tất. Khảo sát trước khi code xác nhận hạ tầng sẵn có khớp gần như hoàn hảo với kỹ thuật sketch đã ghi trong Follow-up Work của spec Đợt 8: `WsGateway` (`apps/api/src/websocket/websocket.gateway.ts`, namespace `/ws` + Redis adapter cho multi-instance) đã có mẫu room-join hoàn chỉnh (`join:live-training` → `client.join(`live-training:${id}`)`), tái dùng y hệt cho `join:chess-analysis` → `chess-analysis:${sessionId}`. Bảng mới `chess*analysis_sessions`(host, tên, trạng thái active/ended, FEN hiện tại, danh sách nước UCI) +`chess_analysis_session_participants`(role host/viewer, unique theo session+user) — mẫu rút gọn 2 bảng từ`live_training_sessions`/`live_training_session_participants`, bỏ hẳn bảng `live_training_attendance`log chi tiết theo giây (không cần cho phạm vi đợt này). Đi nước: client validate trước bằng`chess.js`(như trang phân tích đơn hiện có`ChessAnalysis.page.tsx`, không sửa file này ngoài 1 nút "Tạo phòng cộng tác" mới thêm vào), nhưng **server validate lại toàn bộ** dựa trên FEN đang lưu trong DB (không tin client) bằng chính `chess.js`đã là dependency có sẵn của`apps/api`— bất kỳ ai trong phòng (host lẫn viewer) đều đi nước được, chỉ có reset FEN/kết thúc phòng là hành động riêng của host. **Quyết định kiến trúc đáng chú ý**:`WsGateway`cần gọi vào`ChessAnalysisService`(để validate nước đi + ghi participant) cho các sự kiện`chess-analysis:*`, nhưng `WsGateway`sống trong`WebSocketModule` (`@Global()`) vốn không phụ thuộc module tính năng nào — giải pháp là cho `WebSocketModule`import`ChessModule`(không tạo vòng lặp vì`ChessModule`không import ngược lại`WebSocketModule`, vốn đã global nên không cần import tường minh ở đâu khác), rồi tiêm `ChessAnalysisService`thẳng vào constructor của`WsGateway`— đã khởi động thử API bằng Node 22 để xác nhận không có lỗi circular dependency trước khi commit. Vẫn dùng lại quyền`CHESS*GAME_MANAGE`/`CHESS_GAME_READ`hiện có, không thêm quyền`chess_analysis.*`riêng. **Lưu ý test đáng chú ý**:`chess.js`phiên bản đang dùng **throw Error** cho nước đi bất hợp lệ qua dạng object`{from,to,promotion}`(không trả về`null`/`false`như code hiện có ở`engine.service.ts`giả định) — phải bọc`try/catch`quanh`game.move(...)`trong`ChessAnalysisService.applyMove`mới phát hiện ra qua unit test, việc này **không** sửa lại`engine.service.ts`hiện có (ngoài phạm vi đợt này, có thể là lỗi tiềm ẩn từ trước cần lưu ý nếu gặp lại). Test mới:`chess-analysis.service.spec.ts`(11 test, Jest Node 22). i18n: module`chess.\*`trong codebase từ trước chỉ có bản dịch đầy đủ ở en+vi (5 locale còn lại luôn dùng`defaultValue` tiếng Anh) — đợt này giữ nguyên đúng quy ước đó, không thêm bản dịch cho pl/de/es/lt/cs.

**Trạng thái mục -2:** Đợt 1–11 đều đã **merged vào `main`** (PR #9–#19). Roadmap LearnHouse gốc đã đóng. Các hạng mục còn sót — 7 hạng mục "vận hành & tuân thủ" gốc của Đợt 10 (custom domain, menu builder, landing page builder, custom scripts, đồng tác giả khóa học, khóa bài học theo nhóm, xuất user CSV, SEO mặc định theo tenant) và Follow-up Work riêng của Đợt 11 (presence/con trỏ chuột, chat trong phòng, log tham gia chi tiết, chuyển quyền host, danh sách phòng đang hoạt động) — vẫn nằm trong `docs/research/learnhouse/05-roadmap.md` như các đợt tiềm năng cho tương lai nếu có nhu cầu, nhưng **không còn là ưu tiên hiện tại**: trọng tâm đã chuyển sang roadmap lila (mục -3 dưới đây).

---

## -3. Cập nhật mới nhất (2026-07-26) — Đợt "còn thiếu so với lila (lichess)", tiếp tục tự động triển khai liên tục

Sau khi Đợt 1–11 (còn thiếu so với LearnHouse) hoàn tất, khảo sát tiếp một hệ tham khảo thứ hai: **lila (lichess.org)** tại `D:\code\lila` — hệ thống cờ trực tuyến mã nguồn mở lớn nhất thế giới (84 module Scala, ~1000 dòng route, 43 domain i18n). Lý do: phần LMS của mentingo đã rất mạnh nhưng phần **cờ vua** (trọng tâm sản phẩm "Cờ Vua Học Đường") mới chỉ có 4 bảng/4 permission — một ngân hàng bài tập tĩnh và đấu với máy Arasan, thiếu gần như toàn bộ lớp nghiệp vụ cờ vua bản địa mà lila đã kiểm chứng ở quy mô hàng chục triệu người dùng (bàn cờ tương tác, Study/bài giảng cờ, ngân hàng puzzle có rating Glicko-2, chơi trực tuyến người-với-người, lớp học cờ có tài khoản do giáo viên quản lý, giải đấu Swiss/Arena/Simul, phân tích điểm mạnh-yếu).

**⚠️ Đính chính pháp lý quan trọng**: khác với giả định ban đầu, lila là **AGPL-3.0-or-later**, không phải MIT (`D:\code\lila\LICENSE`/`COPYING.md`). Vì mentingo chạy SaaS multi-tenant qua mạng, AGPL §13 sẽ buộc công khai toàn bộ source nếu copy dù một hàm — áp dụng nguyên chính sách clean-room đã có ở `docs/research/learnhouse/00-cleanroom-policy.md` (LearnHouse cũng AGPL), chỉ đổi hệ tham chiếu sang lila. Dữ liệu puzzle Lichess (CC0) và thuật toán Glicko-2 (public domain, Mark Glickman) được phép dùng trực tiếp.

Kế hoạch chi tiết đã duyệt, lưu tại `C:\Users\duongsinh\.claude\plans\t-i-mu-n-kh-o-s-t-fluttering-meerkat.md` phía máy dev — roadmap 10 đợt (L1–L10, cộng L0 tài liệu khảo sát), mỗi đợt tự động verify rồi commit + push + tạo PR + merge vào `main`, không dừng lại hỏi giữa chừng.

**Lưu ý vận hành phát hiện được khi bắt đầu đợt này**: trong lúc khảo sát/lập kế hoạch lila, một phiên làm việc khác đã tự động hoàn tất toàn bộ Đợt 1–11 nói trên (bao gồm cả Đợt 7 Community Forum vốn đang ở trạng thái working-tree chưa commit tại thời điểm khảo sát bắt đầu) — xác nhận qua `git log`, không có xung đột dữ liệu, `main` đồng bộ sạch với `origin/main` khi đợt lila bắt đầu.

| Đợt    | PR         | Nội dung                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **L0** | #20 merged | Tài liệu khảo sát clean-room `docs/research/lila/` (6 file: cleanroom-policy, architecture-teardown, data-model, feature-matrix, subsystem-notes, roadmap) — không code.                                                                                                                                                                                                                                                                    |
| **L1** | #21 merged | Nền bàn cờ tương tác: vẽ mũi tên/tô ô (shapes, 4 màu qua phím bổ trợ), ký hiệu đánh giá nước đi (glyph), cây biến thay danh sách nước tuyến tính (thêm/nâng/xóa biến, xuất PGN có biến), bàn cờ dựng thế tự do mới tại `/chess/editor` (đặt quân, nhập thành, bắt tốt qua đường, tải/sao chép FEN, mở sang trang phân tích). Hoàn toàn client-side, không bảng/permission mới. Spec: `docs/specs/interactive-chess-board-business-spec.md`. |
| **L2** | #22 merged | Study/Chapter: bài giảng cờ nhiều chương lưu server, 3 chế độ chapter (thường/gamebook/conceal), phân quyền owner/write-member/read-member, clone study. Bảng mới `chess_studies`/`chess_study_chapters`/`chess_study_members` (RLS riêng, mẫu `0183`/`0184`). Spec: `docs/specs/chess-study-business-spec.md`.                                                                                                                             |
| **L3** | #23 merged | Glicko-2 + ngân hàng puzzle nhập CSV có lọc + luyện tập thích ứng + dashboard điểm mạnh/yếu. Bảng mới `chess_puzzles`/`chess_puzzle_attempts`/`chess_ratings`/`chess_rating_history` (RLS riêng, `0185`/`0186`). Spec: `docs/specs/chess-puzzle-rating-business-spec.md`.                                                                                                                                                                   |
| **L4** | #24 merged | Chơi trực tuyến người-với-người: sảnh seek/thách đấu, ván đấu WebSocket với đồng hồ server-authoritative (hết giờ qua BullMQ delayed job), đầu hàng/xin hòa, Glicko-2 theo hạng mục bullet/blitz/rapid. Bảng mới `chess_seeks`/`chess_matches`/`chess_match_moves` (RLS riêng, `0187`/`0188`). Spec: `docs/specs/chess-online-match-business-spec.md`.                                                                                      |
| **L5** | #25 merged | Lớp học cờ: tài khoản do giáo viên quản lý (không cần email thật), tạo hàng loạt từ danh sách tên, mã đăng nhập lớp 5 ký tự hết hạn 15 phút, đặt lại mật khẩu, giải phóng tài khoản, báo cáo tiến độ theo lớp. Mở rộng `users` (3 cột mới) + bảng mới `chess_class_login_codes` (RLS riêng, `0189`/`0190`). Spec: `docs/specs/chess-class-management-business-spec.md`.                                                                     |
| **L6** | #26 merged | Ghép cặp hàng loạt + giải đấu nội bộ: Thụy Sĩ (Swiss)/Arena/Simul, tiebreak Buchholz + Sonneborn-Berger, xuất TRF rút gọn. Bảng mới `chess_tournaments`/`chess_tournament_players`/`chess_tournament_pairings` (RLS riêng, `0191`/`0192`). Spec: `docs/specs/chess-tournament-business-spec.md`.                                                                                                                                            |
| **L7** | #27 merged | Nhập môn: Learn (10 stage × level tĩnh soạn sẵn), luyện tọa độ (client-side), Practice goal có chấm điểm tự động (checkmate-in-N/hòa/chênh lệch vật chất). Bảng mới `chess_learn_progress`/`chess_practice_attempts` + 2 cột mới trên `chess_study_chapters` (RLS riêng, `0193`/`0194`). Spec: `docs/specs/chess-learn-business-spec.md`.                                                                                                   |

**Đợt L1 — chi tiết:** mở rộng trực tiếp `ChessBoard.tsx` (props `shapes`/`onShapesChange` tùy chọn, tương thích ngược 100% với `PgnViewer`/`ChessPlay`/ngân hàng bài tập đang dùng component này); cây biến (`moveTree.ts`) là cấu trúc bất biến thuần (path = mảng id từ gốc), có hook `useMoveTree` bọc React state; thay `Input readOnly` liệt kê nước đi trên `ChessAnalysis.page.tsx` bằng `MoveTreeView`; đọc query param `fen` lúc mount để nhận vị trí từ trang Editor. Editor tự lắp chuỗi FEN bằng hàm thuần (`editorFen.ts`) thay vì dùng `put`/`remove` tăng dần của `chess.js` — API đó validate hợp lệ ngay lập tức nên gây khó chịu khi đang dựng dở thế cờ; chỉ validate bằng `validateFen` (export sẵn của `chess.js`) trước khi cho sao chép/mở sang phân tích. Không đụng `chess_analysis_sessions` (PR #19) — đó là phòng cộng tác tạm thời tách biệt. Test mới: `moveTree.test.ts` (10), `shapes.test.ts` (9), `editorFen.test.ts` (10), Vitest.

**Đợt L2 — chi tiết:** cây biến server-side lưu dạng **danh sách kề phẳng** (`ChessStudyFlatMoveNode: {id,parentId,uci,san,fenAfter,comment?,glyph?,order}`) thay vì cây đệ quy — tránh TypeBox schema đệ quy; `moveTree.ts` (L1) có thêm `flattenMoveTree`/`unflattenMoveTree` để chuyển đổi hai chiều với `MoveTree` phía client, tái dùng nguyên `useMoveTree`/`MoveTreeView`/`ChessBoard`/`BoardShapesOverlay` của L1 cho trang biên tập chapter. `ChessStudyService` tách 3 mức quyền qua hàm private `assertCanRead`/`assertCanWrite`/`assertCanManage` (đọc: công khai hoặc chủ/thành viên bất kỳ/admin; ghi: chủ hoặc thành viên role `write`/admin; quản lý: chỉ chủ hoặc admin) — endpoint sửa/xóa/chapter/thành viên đều gate ở tầng controller bằng `CHESS_STUDY_READ` (thô), KHÔNG dùng `CHESS_STUDY_CREATE`, vì service mới là nơi quyết định đúng-sai theo từng resource; gate bằng permission mạnh hơn sẽ chặn nhầm một thành viên `write` không có quyền tạo study toàn cục. Đổi tên 4 method controller (`createChapter`→`createStudyChapter` …) vì `swagger-typescript-api` đặt tên DTO theo **tên method**, không theo controller — trùng tên với domain course-chapter có sẵn sẽ ghi đè type TS của domain đó (lỗi `tsc` lộ ra ở file hoàn toàn không liên quan). Gamebook/conceal ở phía học sinh triển khai qua `ChessStudyGuidedChapter` — chỉ dẫn theo đúng đường chính (mainline), nước sai bị từ chối im lặng (không cập nhật `fen` vì `ChessBoard` là controlled component). Chưa làm trong đợt này: nhúng study vào lesson khóa học, import/export PGN theo chapter, lượt thích — xem "Follow-up Work" trong spec.

**Đợt L3 — chi tiết:** thuật toán Glicko-2 cài mới hoàn toàn từ đặc tả toán học công khai của Mark Glickman (`apps/api/src/chess/glicko/glicko2.ts`) — unit test đối chiếu đúng ví dụ số minh họa chính thức trong tài liệu gốc (rating 1500/RD 200/volatility 0.06 → sau 3 ván → 1464.06/151.52/0.05999), không nhìn code lila. Mỗi lượt giải puzzle cập nhật Glicko-2 hai chiều (người dùng đấu với "puzzle" như một đối thủ, và ngược lại) qua cùng một hàm `updateRating` thuần. Puzzle chấm bằng cách tái dùng `uciMoveSequencesEqual` đã có (dùng chung với chess-position-line grader của Assignment engine), không viết lại logic so khớp. **Phát hiện đáng chú ý khi verify**: thêm `import { StatisticsService } from "src/statistics/statistics.service"` vào bất kỳ file nào trong `src/chess/` làm vỡ boot với lỗi "circular dependency detected inside FileModule" — `forwardRef()` (cả hai chiều) không sửa được vì đây là vòng lặp `require()` cấp file JS/TS (`chess → statistics → FileService → file/video-upload-notification.gateway → barrel src/websocket → WebSocketModule → ChessModule`, khép vòng), không phải vòng lặp cấp module Nest mà `forwardRef()` xử lý — corrupt decorator metadata của `StatisticsService` trước khi Nest DI kịp chạy. Đã gỡ tích hợp streak khỏi đợt này thay vì ép nối (xem "Follow-up Work" trong spec); chi tiết cơ chế ghi ở wiki nội bộ `nestjs-file-level-circular-require-vs-module-cycle`. Import CC0 dùng CSV dạng chuỗi trong body JSON (đơn giản hóa so với kế hoạch gốc dùng multipart upload), lọc theo rating/motif trước khi BullMQ worker insert theo lô.

**Đợt L4 — chi tiết:** một bảng `chess_seeks` duy nhất gộp cả seek mở lẫn thách đấu đích danh (đơn giản hóa so với kế hoạch gốc 2 bảng riêng). Mở rộng `WsGateway` sẵn có (không tạo gateway riêng, đúng mẫu PR #19): `join:chess-match`/`chess-match:move`/`resign`/`offer-draw`/`accept-draw`, validate nước đi bằng `chess.js`, gateway gọi service rồi tự `emitToRoom` — `ChessMatchService` không import gì từ `src/websocket/` dưới dạng giá trị. Đồng hồ server-authoritative: mỗi nước đi tính lại thời gian còn lại theo `Date.now() - lastMoveAt`; hết giờ phát hiện qua BullMQ delayed job (queue `chess-match-timeout-check`) đặt lại sau mỗi nước với độ trễ đúng bằng thời gian còn lại của bên sắp đi — job kiểm tra `lastMoveAt` chưa đổi trước khi xử lý thua cuộc (tránh race khi vừa đi nước đúng lúc job cũ sắp kích hoạt); do job chạy ngoài ngữ cảnh một WS message, nó phát sự kiện qua `@Inject(REALTIME_PUBLISHER)` (`src/websocket/realtime.publisher.ts` — file không import gì cả nên an toàn tuyệt đối, mẫu đã có ở `course-chat.service.ts`) thay vì gọi thẳng `WsGateway`. Kết thúc ván tự động qua `chess.js` (`isCheckmate/isStalemate/isInsufficientMaterial/isThreefoldRepetition/isDrawByFiftyMoves`) — unit test dùng đúng thế "fool's mate" (1.f3 e5 2.g4 Qh4#) để xác nhận. Glicko-2 cập nhật hai chiều tái dùng nguyên `updateRating` từ L3, hạng mục `bullet`/`blitz`/`rapid` suy tự động từ thời gian cơ bản ván. 22 test Jest mới. Chưa làm: xin hoãn nước, chat trong ván, chống gian lận nâng cao, cờ thư — xem "Follow-up Work" trong spec.

**Đợt L5 — chi tiết:** tài khoản managed dùng **email giả sinh tự động** (`managed.<mã ngẫu nhiên>@class.mentingo.local`) để giữ nguyên ràng buộc `users.email` NOT NULL + unique theo tenant hiện có — tránh phải rà soát lại >10 điểm phụ thuộc vào giả định "email luôn thật" (`validateUser`, magic-link, OAuth, quên mật khẩu...). `firstName`/`lastName` (hiển thị công khai khắp ứng dụng) được đặt bằng **bút danh sinh tự động** (tên quân cờ tiếng Việt + số ngẫu nhiên, ví dụ "Mã 482") — đây là cơ chế "tên thật tách khỏi username": cột mới `realName` chỉ giáo viên/admin xem được, không hiển thị công khai. Đăng nhập bằng mã lớp là **luồng public endpoint riêng** (`POST /auth/class-login`), mô phỏng đúng cấu trúc `handleMagicLinkLogin` đã có (khóa dòng `SELECT ... FOR UPDATE`, dùng một lần, phát hành token giống hệt sau xác thực) — **không sửa** `LocalStrategy`/`AuthService.validateUser` hiện có, giảm rủi ro đụng vào đường code nhạy cảm. Giải phóng tài khoản (`release`) tạo một dòng `create_tokens` (đúng cơ chế bảng/hash token dùng cho luồng mời qua CSV import) và trả token trong response thay vì tự động gửi email — giáo viên tự gửi liên kết, tránh phải kéo `EmailModule` vào module mới. Báo cáo tiến độ lớp tái dùng `ChessPuzzleService.getDashboard` (đổi tham số từ `CurrentUserType` sang `userId: UUIDType` — chỉ dùng `user.userId` nội bộ nên đổi an toàn) gọi cho từng thành viên, cộng với truy vấn trực tiếp `chess_ratings`/`chess_rating_history`/`chess_matches`/`chess_puzzle_attempts` cho phần rating/tỉ lệ thắng — không cần bảng "class stats" riêng. **Phát hiện kỹ thuật đáng chú ý**: thêm 3 cột vào bảng `users` làm `apps/api/src/user/schemas/user.schema.ts`'s `currentUserResponseSchema` (một `Type.Composite` lồng `createSelectSchema(users)` + union ~95 permission literal) vỡ `tsc` với `TS2589: Type instantiation is excessively deep` — sửa bằng cách đổi **outer** composition từ `Type.Composite` sang `Type.Intersect` (không đổi field nào), vì `Intersect` không flatten object type nên rẻ hơn nhiều cho compiler; **cách sửa sai đã thử trước** — nới `permissions` từ union literal sang `Type.Array(Type.String())` — cũng làm `tsc` API sạch nhưng làm vỡ ~15 file phía frontend (qua swagger → `generated-api.ts`) vì `usePermissions`/`Navigation`/`RouteGuard`... đều cần kiểu `PermissionKey[]` chặt; chỉ phát hiện ra sau khi chạy `generate:client` + `lint-tsc-web`, không thấy được nếu chỉ chạy `lint-tsc-api`. Ghi chi tiết ở wiki nội bộ `typebox-composite-ts2589-depth-limit`. 14 test Jest mới cho `ChessClassService`.

**Đợt L6 — chi tiết:** một hàm dùng chung `createMatchesForPairs` (trong `ChessTournamentService`) phục vụ cả 4 luồng ghép cặp (ghép cặp hàng loạt thủ công/tự động theo rating, sinh vòng Thụy Sĩ, ghép cặp Arena, mở bàn Simul) — mỗi cặp gọi `ChessMatchService.createDirectMatch` (**phương thức public mới thêm**, tách từ logic `acceptSeek` đã có ở L4: tạo `chess_matches` + lên lịch job kiểm tra hết giờ), đảm bảo ván đấu do giáo viên ghép cũng tự động xử lý hết giờ giống hệt ván qua sảnh seek — không có luồng ghép cặp nào bỏ sót việc lên lịch job này. Điểm/tiebreak (Buchholz, Sonneborn-Berger) là **pull-based, tính lại từ đầu mỗi lần cần** (bảng xếp hạng, sinh vòng mới, xuất TRF) từ toàn bộ lịch sử `chess_tournament_pairings`, không lưu trạng thái tăng dần — tránh phải để `ChessMatchService.endMatch` gọi ngược vào module mới `chess-tournament` (nếu làm vậy, `chess.module.ts` và `chess-tournament.module.ts` sẽ `require()` lẫn nhau, đúng hình dạng lỗi vòng lặp cấp file đã gặp ở L3); đồng bộ kết quả bằng một bước `syncPairingResults` đọc thẳng bảng `chess_matches` trước mỗi lần tính. `ChessTournamentModule` chỉ import một chiều `ChessModule` (cần `ChessMatchService`/`ChessPuzzleRepository` — export `ChessPuzzleRepository` mới thêm vào `ChessModule`), không có chiều ngược lại. Thuật toán Thụy Sĩ là bản **đơn giản hóa** (chia đôi theo điểm, ghép nửa trên/nửa dưới, tránh ghép lại nếu còn lựa chọn, bye cho điểm thấp nhất chưa từng bye) — không phải chuẩn FIDE Dutch System đầy đủ; xuất TRF cũng là bản rút gọn lấy cảm hứng từ TRF(x), không đảm bảo phần mềm ghép cặp chuyên dụng đọc được — cả hai đơn giản hóa đã ghi trong "Follow-up Work" của spec. 35 test Jest mới (28 cho `ChessTournamentService`/utils thuần: `computeStandings`/`generateSwissPairings`/`pairAdjacentByRating` đối chiếu tay).

**Đợt L7 — chi tiết:** nội dung Learn (10 stage, mỗi stage 1-2 level) là **dữ liệu tĩnh trong mã nguồn** (`packages/shared/src/constants/chessLearn.ts`, gồm cả `solutionUci` — đáp án đúng), không phải nội dung soạn qua UI. Mỗi vị trí FEN/nước đi được **dựng thủ công trên bàn cờ thưa** (ít quân) để có thể xác minh đúng-sai bằng tay thay vì cần engine — riêng level "chiếu hết" tái dùng chính xác thế "fool's mate" đã verify từ L4. **Phát hiện thiết kế quan trọng**: ban đầu định để frontend import thẳng `CHESS_LEARN_STAGES` từ `@repo/shared` cho tiện (danh sách stage/level) — phát hiện kịp thời đây là **rò rỉ đáp án**, vì `@repo/shared` là một package dùng chung cho cả API lẫn web, import bất kỳ phần nào của mảng cũng kéo theo toàn bộ mảng (bao gồm `solutionUci`) vào bundle trình duyệt. Sửa bằng cách thêm trường `levels: {id, completed}[]` vào response `GET /chess-learn/stages` — frontend điều hướng bằng ID lấy từ API, không bao giờ import `CHESS_LEARN_STAGES` trực tiếp; `solutionUci`/`hint` chỉ tồn tại phía server. **Phát hiện lặp lại từ L2**: đặt tên method controller `ChessLearnController.submitAttempt` trùng với `ChessController.submitAttempt` (chấm bài tập cờ tự soạn, có từ trước) — `swagger-typescript-api` đặt tên DTO theo tên method nên ghi đè lẫn nhau, lộ ra qua lỗi `tsc` ở 2 file hoàn toàn không liên quan (`ChessPracticeSolve.page.tsx`, `useSubmitChessExerciseAttempt.ts`); sửa bằng đổi tên thành `submitLearnAttempt` — nhắc lại quy tắc đã ghi từ L2: tên method controller phải duy nhất **toàn bộ** `apps/api/src/`, không chỉ trong class. Practice goal (kích hoạt trường `practiceGoal` tự do đã có từ L2) thêm 2 cột có cấu trúc `practiceGoalType`/`practiceGoalTargetValue`; chấm điểm bằng cách **phát lại toàn bộ chuỗi nước đi UCI học sinh gửi lên bằng `chess.js` phía server** (không tin kết quả client tự báo) rồi kiểm tra điều kiện tương ứng — dùng chung nguyên tắc "server luôn validate lại" đã áp dụng từ L4. 44 test Jest mới (10 cho `practice-goal.utils`, 7 cho `ChessLearnService`, 27 mới/sửa cho `ChessStudyService`).

**Việc cần làm tiếp:** L8 (phân tích điểm mạnh-yếu) → L9 (cộng đồng cờ) → L10 (tường thuật giải đấu), theo đúng thứ tự và phạm vi trong kế hoạch đã duyệt.

**⚠️ Tạm dừng có chủ đích sau L7 (2026-07-27)**: theo yêu cầu rõ ràng của user ở cuối phiên vừa rồi ("làm nốt việc dở dang ở đợt hiện tại trong phiên này, sau đó tổng kết phiên và bàn giao sang phiên khác"), phiên này **dừng lại sau khi L7 merge xong** thay vì tự động tiếp tục sang L8 như các đợt trước. Đây **không phải** thay đổi phạm vi kế hoạch — L8→L9→L10 vẫn còn nguyên trong kế hoạch đã duyệt (`C:\Users\duongsinh\.claude\plans\t-i-mu-n-kh-o-s-t-fluttering-meerkat.md`), chỉ là điểm dừng để bàn giao sang phiên làm việc khác. Phiên tiếp theo có thể tiếp tục ngay từ L8 theo đúng quy trình đã thiết lập (spec → code → verify → commit → push → PR → merge → cập nhật `HANDOVER.md`/`05-roadmap.md`), không cần khảo sát lại.

---

## 0. Cập nhật mới nhất (2026-07-26) — LMS-core: Assignment / Chứng chỉ / Analytics / SEO (PR1–PR5)

Khảo sát LearnHouse (clean-room, xem `docs/research/learnhouse/`) → lên kế hoạch → triển khai tuần tự 5 PR, mỗi PR tự động verify (tsc + eslint sạch, full test suite) rồi commit/push/tạo PR/merge vào `main` không dừng lại hỏi giữa chừng (theo yêu cầu tự động hóa của user). Cả 5 PR đã merge, không còn PR nào đang mở.

| PR           | Nội dung                                                                                                                                                                                                                                                                                                                                  | Merge commit |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| **PR1** (#3) | Sửa bug thống kê (biểu đồ quiz hardcode, cron không ghi `course_students_stats`, gom nhóm tháng bị đè năm) + vá lỗ hổng IDOR chứng chỉ (thêm `share_token`, tra theo token thay UUID)                                                                                                                                                     | `8ca4f2bb`   |
| **PR2** (#4) | Hoàn thiện Assignment engine: sửa được bài đã tạo (trước bấm vào ra màn hình trống), bàn cờ thật thay ô text, upload file nộp bài, trang chấm bài mới, tự thu hồi chứng chỉ khi chấm lại dưới ngưỡng, đồng bộ với hệ thống lesson chung                                                                                                   | `2221a72c`   |
| **PR3** (#5) | QR code chứng chỉ trỏ trang xác minh công khai (hiển thị còn hiệu lực/hết hạn/đã thu hồi) + vá lại đúng lỗ hổng IDOR như PR1 nhưng cho **learning-path certificates** (bị bỏ sót ở PR1)                                                                                                                                                   | `1052db8e`   |
| **PR4** (#6) | 4 báo cáo phân tích sâu theo khóa học (`apps/api/src/analytics/`): phễu hoàn thành bài học, tỷ lệ rơi rụng theo chương (so với chương trước, không phải tổng đăng ký), tốc độ hoàn thành (avg/median + histogram theo khoảng ngày), top 10 học viên theo thời gian học. Hiện ở khối "Deep analytics" trong tab Thống kê của từng khóa học | `14c069ee`   |
| **PR5** (#7) | SEO Tier 1 — không SSR: `robots.txt`/`sitemap.xml` sinh động theo tenant (`apps/api/src/seo/`), trang preview OG/JSON-LD cho bot mạng xã hội khi bot vào `/course/*` (Caddy định tuyến theo User-Agent sang API — "dynamic rendering", không phải SSR thật), thêm `<title>` thật cho `CourseView.page.tsx`                                | `68e7dbd7`   |

**Sửa kèm ngoài kế hoạch (phát hiện khi làm PR5):**

- Thêm nút **Sửa** (trước đây thiếu hoàn toàn) cho `/admin/chess/exercises` (Ngân hàng bài tập cờ vua) — backend đã hỗ trợ `PATCH` đầy đủ từ trước, chỉ là UI chưa từng làm nút Sửa, chỉ có Publish/Xóa. **Chưa merge/PR** — đang ở working tree, chưa commit.
- Vá bug off-by-one trong bảng `drizzle.__drizzle_migrations`: `migrate()` của drizzle-orm chỉ so `MAX(created_at)` toàn bảng, không so hash từng dòng — dòng tracking chèn tay bị lệch `created_at` sang timestamp của migration liền trước khiến API crash lúc boot (`column ... already exists`). Đã sửa `created_at`/`hash` cho 2 dòng bị lệch (id 168, 169) trực tiếp trên DB dev. **Đây là lỗi ở data, không phải code — không cần sửa gì trong repo, nhưng nếu gặp lại crash boot kiểu này ở máy khác, kiểm tra lệch `created_at` theo cách này trước.**

**Ghi chú môi trường quan trọng cho phiên sau:**

- Node **22.15.0** bắt buộc (Node 25 crash JWT) — luôn `export PATH="$HOME/AppData/Roaming/fnm/node-versions/v22.15.0/installation:$PATH"` trước khi chạy pnpm/git hook.
- `drizzle-kit migrate` treo vô hạn trên Git Bash/Windows — áp SQL tay qua `docker exec -i <db> psql`, tự chèn dòng `__drizzle_migrations` (nhớ đúng cả `hash` lẫn `created_at` lấy từ `meta/_journal.json`, xem lỗi off-by-one ở trên).
- Reverse-proxy local dev: `apps/reverse-proxy/Caddyfile`, chạy bằng `caddy run --config Caddyfile` (đã cài sẵn `caddy v2.11.4`). Vào app qua **`https://tenant1.lms.localhost`** (không phải `localhost:5173` trực tiếp — gọi API sẽ lỗi 401 "Missing tenantId" vì thiếu Host khớp tenant trong DB).
- Cẩn thận tích tụ tiến trình: mỗi lần restart `pnpm run dev` (API) qua background bash mà không kill sạch tiến trình cũ, `nest start --watch` cũ vẫn sống ngầm và ăn CPU rất nặng (từng có lúc 6 bản chạy song song làm mọi lệnh `tsc`/`eslint` chậm gấp 10 lần). Trước khi start lại, luôn kiểm tra `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like '*nest start*' }` và kill hết trước.
- Docker Desktop từng tự tắt giữa phiên (không rõ nguyên nhân, đã gặp 2 lần ở 2 ngày khác nhau) — nếu API báo `ECONNREFUSED 127.0.0.1:6379` (Redis), kiểm tra `docker ps` trước, khởi động lại Docker Desktop nếu cần.

**Việc cần làm tiếp (phiên sau bắt đầu từ đây):**

1. Commit + push + tạo PR + merge tính năng nút Sửa `/admin/chess/exercises` (đã code xong, tsc + eslint sạch, chỉ chưa commit — xem mục "Sửa kèm ngoài kế hoạch" ở trên).
2. Không có PR nào khác đang dở — có thể hỏi user muốn triển khai tiếp hạng mục nào (kế hoạch gốc PR1–PR5 đã xong toàn bộ).
3. Dev server hiện đang chạy nền lúc kết thúc phiên này: API (`:3000`), web (`:5173`), Caddy (`:443`), Docker (Postgres/Redis/MinIO) — nếu phiên sau không thấy chúng nữa (máy đã tắt/reboot) thì cần khởi động lại theo đúng thứ tự: Docker → API → web → Caddy.

---

## 0.1 Cập nhật trước đó (2026-07-25) — hoàn thiện 4 hạng mục còn lại (module cờ)

Bốn hạng mục "việc tiếp theo" đã được triển khai xong, verify (tsc + eslint sạch; pre-push hook: **web 202 passed, API 264 passed**) và merge vào `main`:

- **W1 — Lưu ván chơi với máy:** bảng `chess_play_sessions` (RLS), migrations `0164`/`0165`, endpoint `POST/GET /chess/play-sessions`. UI: đồng hồ cờ client-side (5+0 / 10+0 / 15+10), nút **Resign** có xác nhận, tự động phát hiện checkmate/stalemate/insufficient-material/threefold/fifty-move, panel **lịch sử ván** với xem lại từng nước. (Bỏ nút "claim draw" thủ công vì `chess.js` đã tự động hóa mọi điều kiện hòa.)
- **W2 — Chuẩn hóa API client:** xóa `chess-api.ts` hand-rolled; toàn bộ 15 hook cũ + 2 hook mới (`useChessPlaySessions`, `useCreateChessPlaySession`) chạy qua generated swagger client `ApiClient.api.*`.
- **W3 — Tiếng Việt + branding:** thêm `vi` vào `SUPPORTED_LANGUAGES` (i18n, cờ VN, date-fns, `vi/translation.json` đầy đủ 3237 key, parity 100%, + `vi` trong mọi `Record<SupportedLanguages,...>` của API/web). Màu thương hiệu chess tenant → navy `#2B3990`; logo Dương Sinh trong `apps/web/public/brand/`.
- **W4 — E2E:** `data-testid` cho các trang Chess + bàn cờ; factory/handles/flows/specs cho practice, play-vs-engine, admin exercise bank (theo pattern `categories`).
- **Sửa kèm:** route toàn bộ 9 zustand persist store qua `apps/web/app/lib/stores/safeStorage.ts` (chống lỗi `storage.setItem is not a function` khi jsdom teardown giữa test — không đổi hành vi browser).

**Còn 1 bước thủ công:** upload logo `apps/web/public/brand/logo-horizontal-navy.svg` qua **Settings → Branding** trên admin UI (logo platform lưu dạng S3 key, không seed tự động được).

**Lưu ý dev:** branding navy chỉ áp khi **seed mới** — DB dev hiện có (`guidebook`) vẫn giữ màu cũ cho tới khi đổi qua admin UI hoặc re-seed.

---

## 1. Đã làm (tóm tắt)

### Sản phẩm / domain cờ

- Website học–dạy cờ trên Mentingo: **học sinh + phụ huynh** và **giáo viên/HLV**.
- Taxonomy đa chủ đề (không chỉ tactics): nhập môn, luật, thi đấu, khai/trung/tàn, chiến thuật/lược, story, tâm lý, sư phạm…
- **Ngân hàng bài tập** + **ngân hàng ván cờ** (API, admin UI, seed demo).
- **Practice hub** + **Game library** (học sinh).
- **Quiz cờ** trong khóa học: `chess_find_best`, `chess_move_line` (FEN + UCI), chấm điểm + feedback đúng/sai sau Submit.
- **Bàn cờ SVG Staunton** (in-repo, MIT) — thay Unicode.
- **Engine MIT:** Arasan (UCI) + fallback builtin minimax nếu không có binary.
- Trang **Play engine** (`/chess/play`) và **Analysis** (`/chess/analysis`).

### Kỹ thuật chính

| Khu vực     | Nội dung                                                                                                  |
| ----------- | --------------------------------------------------------------------------------------------------------- |
| Shared      | `packages/shared/src/constants/chess.ts` — topics, allowlist MIT, engine levels                           |
| Permissions | `chess.exercise.*`, `chess.game.*` (backfill khi API start)                                               |
| API         | `apps/api/src/chess/` — exercises, games, attempts, engine (Arasan UCI)                                   |
| Web         | `apps/web/app/modules/Chess/` — board, admin banks, practice, play, analysis                              |
| Client HTTP | `apps/web/app/api/chess-api.ts` (axios qua `ApiClient.instance`; chưa bắt buộc regenerate swagger client) |
| DB          | Migrations `0162_chess_exercise_game_banks`, `0163_enable_chess_tenant_rls`                               |
| Specs       | `docs/specs/chess-*-business-spec.md`, `chess-engine-arasan-business-spec.md`                             |
| Seed        | `apps/api/src/seed/seed-chess-data.ts`, `seed-chess-only.ts`                                              |

### Bug đã xử lý giữa chừng

- Login 500 khi API down → layout/`useCompanyInformation` không crash public shell.
- Practice empty + toast permission → query `publishedOnly` boolean string; re-login sau backfill permission.
- RLS thiếu trên bảng chess → migration 0163.
- Quiz đi đúng vẫn “missing questions” → Zod strip `chessResponses` → thêm vào schema.
- Quiz không hiện đúng/sai → feedback UI trên `ChessMoveQuestion`.
- Engine 404 → API chưa rebuild/restart; `Cannot POST /api/chess/engine/*`.
- Arasan: dùng **`arasanx-64.exe`** (UCI), không dùng `arasan-64.exe` (GUI).

---

## 2. Cấu hình quan trọng

### Env API

**File:** `apps/api/.env`

```env
ARASAN_PATH="C:\Program Files\Arasan\26.0\arasanx-64.exe"
```

- Binary UCI: `arasanx-64.exe` (cùng folder cài Arasan 26.0).
- Không set `ARASAN_PATH` → fallback engine builtin (MIT, yếu hơn, đủ demo).
- Sau khi sửa env: **restart API** (Node **22**, tránh Node 25 với JWT/SlowBuffer).

### Routes web

| URL                      | Mô tả                     |
| ------------------------ | ------------------------- |
| `/chess/practice`        | Luyện bài từ bank         |
| `/chess/practice/:id`    | Giải 1 bài                |
| `/chess/games`           | Thư viện ván              |
| `/chess/games/:id`       | Xem PGN                   |
| `/chess/play`            | Chơi máy (Arasan/builtin) |
| `/chess/analysis`        | Phân tích thế cờ          |
| `/admin/chess/exercises` | Admin bank bài tập        |
| `/admin/chess/games`     | Admin bank ván            |

### API engine

- `GET /api/chess/engine/status`
- `POST /api/chess/engine/bestmove`
- `POST /api/chess/engine/analyze`

---

## 3. Lệnh hữu ích

```bash
# Node 22 (khuyến nghị)
fnm use 22.15.0

pnpm --filter @repo/shared build
pnpm --filter=api db:migrate
pnpm --filter=api exec tsx ./src/seed/seed-chess-only.ts   # seed bank nếu trống

# Dev
pnpm dev
# hoặc API: nest build + node dist/src/main.js (cwd apps/api, load .env)
```

Login seed thường: `admin+tenant1@example.com` / `password` (tenant `tenant1.lms.localhost`).

---

## 4. Việc tiếp theo (gợi ý ưu tiên)

**Đã xong (2026-07-25):** ✅ Lưu ván chơi + clock + resign/draw UI · ✅ chuẩn hóa API client (bỏ `chess-api.ts`) · ✅ E2E Playwright (practice/play/admin) · ✅ branding “Cờ Vua Học Đường” + i18n vi đầy đủ.

Còn lại:

1. **Nội dung sản phẩm**

   - Soạn 1–2 khóa demo HS + GV (video/text + quiz cờ + embed PGN).
   - Mở rộng bank (luật, sư phạm, tactics…) và learning paths theo khối lớp.

2. **Engine / UX**

   - Production: luôn cài Arasan + `ARASAN_PATH` trong deploy (Docker mount binary).
   - Gắn “thắng máy level N” vào curriculum completion (optional).
   - Chạy E2E chess trực tiếp trong CI (specs đã có, chưa chạy verify trên máy local do tải).

3. **Deploy (chuẩn bị)**

   - Upload logo navy qua admin UI (bước thủ công, xem mục 0).
   - Set `ARASAN_PATH` cho môi trường prod (nếu muốn engine mạnh; không set → builtin MIT).
   - Migrations tự chạy khi API start (`0164`/`0165` đã sẵn) — không cần thao tác tay.
   - Verify domain `covuahocduong.com` trỏ đúng tenant; i18n mặc định = `vi`.

4. **Không làm (policy)**
   - Stockfish / Chessground GPL.
   - Multiplayer realtime kiểu Lichess (chưa trong scope).

---

## 5. Rủi ro cần nhớ

| Rủi ro                         | Ghi chú                                 |
| ------------------------------ | --------------------------------------- |
| Node 25                        | JWT/`SlowBuffer` crash — dùng Node 22   |
| API không restart              | Route engine 404 / code cũ              |
| Sai binary Arasan              | GUI vs UCI (`arasanx-64`)               |
| JWT cũ sau backfill permission | Logout/login lại                        |
| `chess-api` hand-rolled        | Regenerated client khi contract ổn định |

---

## 6. File/entry points nhanh

```
packages/shared/src/constants/chess.ts          # topics, engine levels, play outcomes/end-reasons, time controls
packages/shared/src/constants/permissions.ts    # CHESS_*
packages/shared/src/constants/languages.ts       # + VI
apps/api/src/chess/                              # module chính (+ play-sessions endpoints)
apps/api/src/chess/engine/                       # Arasan + builtin
apps/api/src/storage/migrations/0164_*, 0165_*  # chess_play_sessions + RLS
apps/api/.env                                    # ARASAN_PATH
apps/web/app/modules/Chess/                      # + Play/useChessClock.ts, Play/PlaySessionsPanel.tsx
apps/web/app/api/queries|mutations/useChess*     # đã dùng generated client (chess-api.ts đã xóa)
apps/web/app/lib/stores/safeStorage.ts           # crash-safe zustand persist storage
apps/web/app/locales/vi/translation.json         # locale vi đầy đủ
apps/web/public/brand/                           # logo Dương Sinh
apps/web/e2e/{data,factories,flows,specs}/chess/ # E2E chess
docs/specs/chess-*-business-spec.md
docs/specs/chess-engine-arasan-business-spec.md
```

Hết phần handover kỹ thuật session này.
